use seamless_core::*;

fn main() {
    unsafe {
        let rx = 0.0;
        let ry = 0.0;
        let rz = -1.5707963267948966; // -90 deg
        
        let q_rust = [0.0; 4]; // We'll just call the C++ function!
    }
}
