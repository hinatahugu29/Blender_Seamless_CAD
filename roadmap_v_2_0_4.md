<!-- DOC-STATUS -->
> **状態: 歴史的資料** — 「真の円弧エッジのネイティブ生成」計画。**目的は達成済み**（`INVENTORY_V8_1_5_1.md` §2 の Arc 型、§4 の arc / circle 描画ツール）。
> 版番号 V2.0.4 は現行の 8.1.x とは無関係。
>
> 全体の分類は `CLAUDE.md` §6 にある。**このバナーを消さないこと。**

---

# Blender_CAD V_2_0_4 開発ロードマップ（A案：真の円弧エッジのネイティブ生成）

本ドキュメントは、2Dスケッチから3Dモデルを作成する際の円弧・円のカクつき問題を根本的に解決するため、OpenCASCADEの真の円弧（Arc）および円（Circle）エッジをネイティブ生成する「A案」の段階的実装計画です。

---

## 開発フェーズ一覧

### フェーズ 1：データ構造とデータ受け渡しの設計（Python ⇔ Rustブリッジ）
Python側のスケッチ確定処理を拡張し、C++側に「直線」「円弧」「円」の幾何情報を引き渡せるようにします。

* **[1-1] スケッチ情報の抽出処理（Python）**
  * `modal_sketch.py` の `finalize_sketch` が頂点をただ繋ぐのではなく、スケッチ中の各エンティティ（`Line`, `Arc`, `Circle`）から、以下の制御点を抽出するロジックを実装します。
    * **直線（Line）：** 始点、終点
    * **円弧（Arc）：** 始点、中間点、終点
    * **円（Circle）：** 中心、半径、法線
* **[1-2] シリアライズデータ構造の定義**
  * C++へ送るために、これらを構造化データ（JSON文字列、またはC++側へ一括で流せるフラットな数値配列のセット）へシリアライズする処理を Python と Rust ブリッジ層に定義します。

### フェーズ 2：OpenCASCADE側でのネイティブ幾何構築（C++）
C++側で、受け取った幾何セグメント情報を元に完全に滑らかなワイヤー構造を合成します。

* **[2-1] セグメントパーサーの実装（`occ_core.cpp`）**
  * C++側の `make_curve_or_surface` を拡張し、新シリアライズフォーマットから「直線」「円弧」「円」のデータをパース・展開する処理を実装します。
* **[2-2] エッジ・ワイヤー構築ロジックの実装**
  * パースした各セグメントに対応するOpenCASCADE APIを呼び出し、ネイティブな `TopoDS_Edge` を個別に生成します。
    * `LINE` ➡ `BRepBuilderAPI_MakeEdge`
    * `ARC` ➡ `GC_MakeArcOfCircle`
    * `CIRCLE` ➡ `gp_Circ` + `BRepBuilderAPI_MakeEdge`
  * 生成したエッジを `BRepBuilderAPI_MakeWire` で結合し、単一の `TopoDS_Wire`（ワイヤー）として合成します。
* **[2-3] 面（Face）の再生成と向き自動修正**
  * 合成したワイヤーを用いて `BRepBuilderAPI_MakeFace` で面を作成します。V_2_0_3で実装した `ShapeFix_Face::FixOrientation()` による内枠/外枠の向きの自動修正もここに適用し、穴あき面も滑らかに処理します。

### フェーズ 3：後続モディファイアとの統合と挙動確認
生成された「真の円弧面」が、既存の3D化処理やモディファイアで正しく動作するか検証します。

* **[3-1] 3D化処理の動作検証**
  * 押し出し（Extrude）、回転（Revolve）、スイープ（Sweep）などの既存処理にこの滑らかな面を流し込み、3D形状になってもエッジが滑らかに追従することを確認します。
* **[3-2] ブーリアン・フィレット処理の検証**
  * 真の円筒面に対して、他のパーツとのブーリアン（Cut/Fuse）や、円形エッジへのフィレット・面取りがエラーなく実行できるかを確認します。

### フェーズ 4：品質検証とエクスポートテスト
最終的なデータ品質と実用性のテストを行います。

* **[4-1] ビューポート描画への影響確認**
  * 滑らかな形状がBlenderのビューポート上で美しく描画されるか、メッシュの品質（deflection）と描画速度のバランスをテストします。
* **[4-2] STEP形式でのエクスポート確認**
  * 完成した3D形状をSTEPファイルで書き出し、他のCADツール（Fusion 360やSolidWorksなど）にインポートした際に、完全に滑らかな「本物の円柱/円弧」として編集可能な状態で認識されるかを最終確認します。
