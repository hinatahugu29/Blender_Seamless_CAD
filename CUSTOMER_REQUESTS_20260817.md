# ユーザーフィードバック・質問対応記録 (2026-08-17)

2026年8月17日にユーザー（購入者）から届いた問い合わせ・要望の記録、技術的背景の分析、および回答案と今後の改修タスクの整理。

---

## 1. 問い合わせ原文 (2026-08-17)

> 素晴らしいアドオンを作成していただきありがとうございます。  
> Seamlessを学習して行く中で疑問点がでてきたため問い合わせさせていただきます。  
> Blenderは長年使用していますがCADはほぼ素人のため初歩的な質問が多いかと思いますがよろしくお願いいたします。  
>
> 1. **キーマップを変更しない方法はあるでしょうか。**  
>    普段はAlt+マウスの各ボタンドラッグやQWERを使用する業界標準キーマップをベースに操作しています。  
>    キーマップの上書きはないとのことでしたが、Selection ModeやStart Sketchを実行するとBlender標準にキー操作が変わってしまうため困っています。  
>    またSketchModeでテンキーのビューポート変更ショートカットが効かなくなるため間違って画面を回してしまったときに不便に感じています。  
>    どちらも元のキーマップのまま使う方法はありますでしょうか。  
>
> 2. **Sketch Modeで頂点が吸着してしまうのをOFFにする方法はあるでしょうか。**  
>    頂点同士を近づけると吸着マージのような動作が起きますが、意図せず起きてしまうことのほうが多いため普段はOFFにしたいです。  
>    また長方形を作成後に選択モードでどれかの頂点をクリックすると他の頂点がその頂点に近づいていき一つにまとまるのもこの機能の影響でしょうか？  
>
> 3. **Edit Sketchのときに元のSketchを非表示にする、もしくは別の色にする方法はあるでしょうか。**  
>    Edit Sketchをしたときに元のSketchが同じ色で表示されているため、どちらを編集してよいのかわからなくなることが多いです。  
>    視認性を上げる方法はあるでしょうか。  
>
> 4. **プレビュー先を切り替える、もしくはSeamlessモードを終了する方法はあるでしょうか。**  
>    Seamlessは同時に1つしか使用できないようですが、普段2つ以上のBlenderを同時に開いて参考用モデルを表示していることが多いです。  
>    そのため最初に起動したSeamlessで作成しているモデルしかプレビュー表示されないのが不便に感じています。  
>    同時にプレビュー表示は難しいと思うので、片方を終了させてからもう片方で起動する方法でも良いので2つのシーンを扱う方法があるか知りたいです。  
>
> 以上となります。よろしくお願いいたします。

---

## 2. 各問い合わせの分析と技術的背景

### Q1. 業界標準キーマップ（Industry Compatible）の維持 & テンキー視点変更
- **現状のメカニズム:**
  - `modal_sketch.py` や `operators/selection.py` などのモーダルオペレータが実行中の間、イベントループでBlender標準の `MIDDLEMOUSE`（回転/パン）や `W/E/R` などをハードコードで判定・処理している、あるいはBlenderの標準イベントに `PASS_THROUGH` せず消費（ブロック）してしまっている。
  - Industry Compatible Keymap（`Alt + LMB` で回転、`Alt + MMB` でパン、`Alt + RMB` でズーム等）や、テンキーの視点切り替え（`NUMPAD_1/3/7`）がモーダル内で明示的にパススルーされていない。
- **改善タスク:**
  - モーダルオペレータの `modal()` 関数内で、アドオン固有の操作（LMBクリック・ドラッグ等）以外のナビゲーション系イベント（`event.type.startswith('NUMPAD_')` や `Alt` 組み合わせキーなど）を `PASS_THROUGH` でBlender本体に透過させる。
  - 業界標準キーマップ環境でのモーダル操作の互換性テスト。

---

### Q2. スケッチの自動吸着（Coincident）OFF化 & 長方形頂点クリック時の縮退バグ
- **現状のメカニズム:**
  - `sketch/actions/snapping.py` で近接頂点へのスナップおよび自動Coincident拘束の適用が行われている。現状はスナップのグローバルON/OFFトグルまたは閾値設定がUI上に公開されていない。
  - **長方形の頂点クリックで1点にまとまる現象:** スケッチソルバー（`ezpz`）において、選択操作やドラッグ操作時の初期座標更新で拘束の自由度が縮退解（全頂点が同一点に収束する解）に落ちるバグの疑いがある。
- **改善タスク:**
  - スケッチパネル（Nパネル/ヘッダー）に「Auto Snap / Auto Coincident」のON/OFF切り替えプロパティを追加。
  - 長方形作成後の頂点選択・ドラッグ時の縮退挙動を回帰テストとして再現・修正。

---

### Q3. Edit Sketch 時の二重表示・視認性（元のSketchとの重複）
- **現状のメカニズム:**
  - 既存のスケッチフィーチャーを「Edit Sketch」で編集モードに入った際、Blender上の既存メッシュ/カーブオブジェクト（または前回のベイク・プレビュー結果）が表示されたまま、その上に編集中のスケッチ（GPUオーバーレイ描画）が重なって描画されている。
  - 色や表示非表示の切り替えがないため、どちらが編集中のアクティブエンティティか判別しづらい。
- **改善タスク:**
  - スケッチ編集モード（Start Sketch / Edit Sketch）突入時に、編集対象の元フィーチャーのプレビュー/メッシュを一時的に非表示（`obj.hide_viewport = True`）にするか、色・不透明度（Ghosting / X-ray）を変更して視覚的コントラストをつける。
  - スケッチ終了（Finish / Cancel）時に元の表示状態へ復帰させる。

---

### Q4. 複数Blender起動時のサーバー競合 & プレビュー切り替え
- **現状のメカニズム:**
  - 現在、`cad_server.exe` は固定ポート `8080` を使用してTCP通信を行っている。
  - 1つ目のBlenderが `cad_server.exe` を起動してポート8080を占有するため、2つ目のBlenderを起動すると「ポートが既に使われている」として新規起動できず、既存の別インスタンスのサーバーへ相乗りしようとして競合・プレビュー無効化が発生する。
- **改善タスク:**
  - **短期的対応（ユーザー向け運用回避策）:**
    - 2つ目のBlenderではSeamless CADを使用しない（参考モデルの確認のみ）場合、アドオンを無効化するか、Bake Mesh（メッシュ化）した状態で作業する。
    - または「Server Stop / Restart」ボタンをPreferencesまたはNパネルに設けて、使用するBlender側からポートを掴み直せるようにする。
  - **中長期的対応（根本解決）:**
    - 各Blenderプロセスごとに動的ポート割り当て（空きポート探索: 8080, 8081, 8082...）を行い、BlenderのPIDごとに個別の `cad_server.exe` プロセスを起動・通信させるマルチインスタンス完全分離設計への移行。

---

## 3. ユーザー（お客様）向け返信文案

```markdown
〇〇 様

Seamless CAD をご利用いただき、また大変貴重で具体的なご意見・ご質問をいただき誠にありがとうございます。
Blenderに精通されている視点からのフィードバックは、アドオンの操作性向上において非常に助かります。

いただいた4点につきまして、現状の仕様と今後の対応方針について回答いたします。

---

### 1. キーマップ（業界標準キーマップ・テンキー操作）について
【現状と原因】
Selection Mode や Sketch Mode 中は専用のモーダル操作（マウスによる選択・作図）が起動しており、内部で一部のキー操作をブロック・独自処理しているため、Industry Compatible（業界標準キーマップ）のAltドラッグや、テンキーによる視点変更（1, 3, 7キーなど）が透過されない状態となっております。

【今後の対応】
次期アップデートにて、モーダル実行中であってもBlender標準の視点ナビゲーションやテンキーのショートカットを阻害しないよう、イベントのパススルー（透過）処理を改修いたします。

---

### 2. スケッチモードでの頂点吸着（スナップ）および長方形の頂点収束について
【現状】
現在、頂点同士を近づけた際に自動的に一致拘束（Coincident）を付与するスナップ機能が常時ONとなっております。
また、長方形の頂点をクリックした際に他の頂点まで引き寄せられて1点にまとまってしまう現象は、拘束ソルバーの計算時に意図しない縮退（ゼロ距離への収束）が発生している不具合の可能性が高いです。

【今後の対応】
・スナップ機能のON/OFF切り替え（または一時無効化キー）をUIに追加いたします。
・長方形の頂点選択時に形状が崩れて1点にまとまってしまう現象につきましては、至急原因を調査し修正パッチを適用いたします。

---

### 3. Edit Sketch時の元スケッチとの重なり・視認性について
【現状】
スケッチ編集（Edit Sketch）を開始した際、確定済みのモデル/プレビューが同じ見た目で残ったまま編集用オーバーレイが重ねて描画されるため、編集対象が見分けにくくなっております。

【今後の対応】
Edit Sketch 中は、編集対象以外の元のプレビューを自動的に半透明（ゴースト表示）または一時非表示にし、現在編集しているスケッチ線（色・ハイライト）がはっきりと区別できるよう描画処理を改善いたします。

---

### 4. 2つ以上のBlenderを起動した際のプレビューについて
【現状の制限】
Seamless CAD は高速な幾何計算を行うため、バックグラウンドで専用の幾何計算サーバー（cad_server）と通信しています。
現バージョンでは通信ポート（8080番）が固定されているため、最初に起動したBlenderがサーバーを専有し、2つ目に起動したBlender側からは正常にプレビューが更新されない仕様となっております。

【現在の回避策】
・参考モデルを表示する側のBlenderでは、形状を「Bake Mesh（メッシュ化）」して通常オブジェクトとして表示していただくか、一時的にアドオンをOFFにしていただくことで干渉を防ぐことができます。

【今後の対応】
複数のBlenderを同時に開いた場合でも、それぞれのウィンドウごとに自動で空きポートを割り当てて個別に幾何計算サーバーを起動・プレビューできるよう、マルチインスタンス対応を進めてまいります。

---

いただいたご意見はすべて今後のアップデート計画（ロードマップ）に組み込み、順次改善してまいります。
進捗や修正版のリリースにつきましては、Superhiveのアップデート通知およびチェンジログにてお知らせいたします。

引き続きお気づきの点やご要望がございましたら、いつでもお気軽にお知らせください。
今後とも Seamless CAD をよろしくお願いいたします。
```

---

## 4. 今後の開発タスク一覧（優先度別）

| 優先度 | 項目 | 影響範囲 | 担当モジュール |
|---|---|---|---|
| 状態 | 優先度 | 項目 | 影響範囲 | 担当モジュール |
|---|---|---|---|---|
| **一部済** | **高 (Bug)** | 長方形頂点クリック時の縮退・収束バグ | スケッチの安定性 | `state_select.py`, `ezpz` |
| **済** | **高 (UX)** | テンキー視点変更＆業界標準キーマップ対応 | 全体操作感 | `utils.py`, 各モーダル |
| **済** | **中 (UX)** | スケッチ自動スナップのON/OFFトグル追加 | スケッチ作図 | `state_base.py`, `properties.py`, UI |
| 未着手 | **中 (UX)** | Edit Sketch時の元モデル/スケッチのディミング | 編集視認性 | `drawing.py`, `operators/` |
| 未着手 | **中 (Arch)** | 複数Blenderインスタンス対応（動的ポート割り当て） | マルチウィンドウ環境 | `core_bridge.py`, `main.rs` |

### 2026-08-17 の作業でどこまで進んだか

ブランチ `fix/customer-feedback-20260817`。回帰テストは 44 passed / 0 failed。
ただし**回帰テストが見ていない領域**（`CLAUDE.md` §3 のチェックリスト C・D・H）に
今回の変更は正面から入っている。実機確認が要る。

- **Q1（キーマップ）— 完了。** `utils.is_viewport_nav_event()` に判定を集約し、
  5つのモーダル（sketch / selection / transform ×2 / offset_pick / visual_snap）から呼ぶ。
  Selection Mode は Alt を自前のギズモ修飾キーに使っているため、
  **ギズモ軸にも制御点にも当たっていないときだけ** Alt+LMB を回転として本体へ渡す。
- **Q2（スナップ）— トグル完了、縮退は未再現。** `sketch_snap_vertex` を追加（既定 ON）。
  Ctrl 押下で1クリックぶんバイパス。縮退バグは上記のとおり原因が文書と違っており、
  ドラッグ跳ねの修正は入れたが「1点にまとまる」現象自体は未再現・未修正。
- **Q3・Q4 — 未着手。**

**実機で確認すべきこと（未検証）:**
1. Industry Compatible キーマップで、Sketch / Selection 中に Alt+LMB で画面が回るか。
2. Selection Mode で Alt+LMB のギズモ軸ドラッグが従来どおり効くか（1と両立しているか）。
3. Sketch / Selection 中にテンキー 1・3・7 で視点が切り替わるか。
4. Vertex Snap を OFF にして長方形の角をドラッグしたとき、1点にまとまるかどうか。
   まとまらなければ吸着経路が原因と確定できる。

---

## 5. コードベース詳細調査報告 (2026-08-17 実地確認)

※ コードの改訂・発行は行わず、正本コード（`Blender_CAD_V_8_1_5_1`）の静的解析および実地調査を実施した結果です。

---

### ① キーマップ問題（Industry Compatible & テンキー操作のブロック）

#### 該当コード箇所
- [`modal_sketch.py:115-144`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/sketch/modal_sketch.py#L115-L144) (`SEAMLESS_OT_SketchDrawTool.modal`)
- [`modal_selection.py:264-297`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/modal_selection.py#L264-L297) (`SEAMLESS_OT_SelectionModal.modal`)
- [`operators/transform.py:129-140`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/operators/transform.py#L129-L140) (`SEAMLESS_OT_TransformModal.modal`)

#### 現状の実装と不具合の原因
1. **Industry Compatible（業界標準キーマップ）の競合:**
   > **【2026-08-17 追記・修正済み】テンキーが効かない直接原因が判明した。**
   > `modal_sketch.py:142` のパススルーは `'PAD1'`〜`'PADPERIOD'` と書かれていたが、
   > **Blender の `bpy.types.Event.type` にその識別子は存在しない**（正しくは `NUMPAD_1`）。
   > 綴り違いなので例外にならず、`in` 判定が常に偽になり、
   > **書いてあるのに一度も発火していなかった**。Blender 5.1 の enum を直接列挙して確認済み。

   - 業界標準キーマップでは、視点操作が `Alt + 左ドラッグ`（回転）、`Alt + 中ドラッグ`（パン）、`Alt + 右ドラッグ`（ズーム）に割り当てられている。
   - しかし `modal_sketch.py:132` では `event.alt` を「マウスホイールによるグリッドステップ変更」にのみ使用しており、`LEFTMOUSE` イベント受信時（149行目）に `event.alt` の押下状態をチェックせずに作図・ドラッグ処理へと進んでしまう。
   - このため、ユーザーが `Alt + 左ボタンドラッグ` で画面を回そうとすると、画面が回転せずスケッチのクリック・ドラッグとして誤認識・消費されてしまう。
2. **テンキー視点変更ショートカット（NUMPAD 1, 3, 7等）の欠落:**
   - `modal_sketch.py:142` では `PAD1`〜`PAD0`, `PADPERIOD` のパススルーが書かれているが、`modal_selection.py:295`（選択モード）には `MIDDLEMOUSE` 系のパススルーしか存在せず、テンキーイベントのパススルー記述が完全に欠落している。
   - その結果、Selection Mode 中にテンキーを押してもBlender本体にイベントが届かず、正面・側面・上面への視点リセットが動作しない。

#### 改修に向けた設計方針
- 各モーダル（Sketch, Selection, Transform）のイベント冒頭に、以下を共通で判定・パススルーするガード節を追加する：
  ```python
  # Industry Compatibleの視点ナビゲーション（Alt + 各マウスボタン）
  if event.alt and event.type in {'LEFTMOUSE', 'MIDDLEMOUSE', 'RIGHTMOUSE'}:
      return {'PASS_THROUGH'}
  # テンキーによる視点切り替え
  if event.type.startswith('PAD') or event.type in {'NUMPAD_1', 'NUMPAD_2', 'NUMPAD_3', 'NUMPAD_4', 'NUMPAD_5', 'NUMPAD_6', 'NUMPAD_7', 'NUMPAD_8', 'NUMPAD_9', 'NUMPAD_0', 'NUMPAD_PERIOD'}:
      return {'PASS_THROUGH'}
  ```

---

### ② スケッチ自動スナップOFF & 長方形頂点クリック時の縮退・収束バグ

#### 該当コード箇所
- [`sketch/states/state_rectangle.py:85-94`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/sketch/states/state_rectangle.py#L85-L94) (`StateRectangle._commit_rectangle`)
- [`sketch/states/state_select.py:15-120`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/sketch/states/state_select.py#L15-L120) (`StateSelect.handle_mouse_move` - 単一移動ロジック)
- [`sketch/states/state_line.py:15-56`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/sketch/states/state_line.py#L15-L56) (`StateLine.handle_left_click_press`)
- [`sketch/states/state_base.py:88-140`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/sketch/states/state_base.py#L88-L140) (`StateBase` のインファレンス・中点スナップ)

#### 現状の実装と不具合の原因
1. **自動吸着（Coincident）の無効化手段が無い点:**
   - 線を描画する際、カーソル近傍に既存頂点があると `props.sketch_hover_point_id`（>= 0）がセットされ、クリック時に新規点を作らず既存頂点の ID をそのまま再利用（トポロジー結合）する仕様になっている。
   - スナップを一時的にバイパスする修飾キー（`Ctrl` や `Alt`）や、スナップ有効/無効のグローバルトグルがUIに備わっていないため、意図しないマージが頻発する。
2. **長方形の頂点クリック・ドラッグで1点に縮退するバグの根本原因:**

   > **【2026-08-17 実測により訂正】以下の「縮退解」説は誤りだった。**
   > ヘッドレスで長方形（寸法拘束なし・H/V のみ）を組み、ソルバを直接叩いて確認した。
   >
   > - **単一ドラッグ**（`drag_pt_id` + `FIXED` あり）: 角を動かすと残り3頂点が正しく追従し、
   >   長方形のままリサイズされる。8ステップ動かしても崩れない。
   > - **複数ドラッグ**（アンカーなしの `solve_gcs_external(props, context)`）: 全頂点を
   >   平行移動してから解いても、10回繰り返して形状は不変。
   >
   > つまり **ezpz は縮退解に落ちていない**。寸法拘束の欠如は縮退の原因ではない。
   > 再現用スクリプトの手順は本節末尾の「実測メモ」を参照。

   実測で見つかった実際の欠陥は別の2つ：

   - **クリック位置の基準ずれ（修正済み）**: `modal_sketch.py:210-215` はクリック時、
     ホバー中の頂点があると渡す座標をその頂点の座標へ差し替える。`state_select.py` は
     それをそのまま `_last_mouse_pos` の初期値にしていた。以降の `delta` は
     生カーソル座標との差分なので、**ドラッグ1フレーム目でクリック誤差ぶん
     （ホバー判定は画面上15px まで拾う）図形全体が飛ぶ**。
   - **作成直後に全選択が残る（未修正）**: `state_rectangle.py:95-96` が4頂点・4線分すべてを
     選択状態のまま残す。`state_select.py:186` の「選択済みの頂点をドラッグしたら選択を維持」が
     成立するため、**どの角をクリックしても単一リサイズではなく矩形全体の平行移動**になる。
     角を掴んだつもりが図形ごと動くので、ユーザーの体感としては「頂点をクリックすると形が変わる」。

   **1点への収束そのものはヘッドレスで再現できていない。未検証。**
   実機（GUI）でのみ起きる経路が残っている可能性がある。次に見るべきは
   ドラッグ中に `hit_pos` が他頂点へ吸着し、`pt.co = hit_pos` で
   隣の角と同一座標になる経路（Vertex Snap を OFF にすれば回避できるはず）。

#### 改修に向けた設計方針
- **長方形の形状維持:**
  - 長方形作成時に暗黙的な対向辺連動（コーナー点ドラッグ時は直交する2頂点の X/Y を追従させる矩形リサイズロジック）を `state_select.py` の単一ドラッグ処理に追加する。
  - または長方形生成時に「幅・高さ」の参照寸法拘束を自動付与する。
- **スナップ制御の追加:**
  - Nパネルおよびヘッダーに `props.sketch_snap_coincident` のON/OFFチェックボックスを追加。
  - 作図中に `Alt` または `Shift` キーを押している間はスナップ（`hover_point_id` の検出）を一時的にスキップする処理を `state_base.py` に追加。

---

### ③ Edit Sketch 時の元スケッチ・ソリッドとの二重表示・視認性問題

#### 該当コード箇所
- [`operators/management.py:1035-1078`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/operators/management.py#L1035-L1078) (`SEAMLESS_OT_EditSketch`)
- [`drawing.py:400-550`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/drawing.py) (GPUオーバーレイ・ソリッド描画)
- [`sketch/sketch_draw.py:200-400`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/sketch/sketch_draw.py) (スケッチ要素描画)

#### 現状の実装と不具合の原因
1. `Edit Sketch` オペレータが起動した際、退避されていたスナップショットから点・線・拘束が復元されてスケッチ描画ループ（`sketch_draw.py`）が開始される。
2. しかし、この間も 3D 空間上には以前計算された確定済みソリッドのプレビュー描画（`drawing.py`）が完全に不透明な状態で描画され続けている。
3. さらに、スケッチ線の描画色と既存メッシュ・エッジの色が近く、同座標に重なって描画されるため、ユーザーから見ると「どれが編集中のスケッチで、どれが確定形状のエッジか」が全く見分けられない。

#### 改修に向けた設計方針
- `props.is_sketch_active` が True の期間中：
  - `drawing.py` のソリッド描画（B-Repメッシュおよびエッジ）の透過度（Alpha）を 0.15〜0.2 程度に自動減衰（ディミング/ゴースト表示）させる。
  - または、編集対象フィーチャー（およびその下流フィーチャー）のプレビュー描画を一時的にスキップする。
- スケッチ確定（Finish Sketch）またはキャンセル時に透過度を通常（1.0）へ復帰させる。

---

### ④ 複数Blenderインスタンス起動時のサーバー競合問題（ポート8080）

#### 該当コード箇所
- [`core_bridge.py:31`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/core_bridge.py#L31) (`_SERVER_PORT = 8080`)
- [`core_bridge.py:580-631`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/core_bridge.py#L580-L631) (`start_server()`)
- [`src_rust/src/main.rs:16-55`](Blender_CAD_V_8_1_5_1/src_rust/src/main.rs#L16-L55) (`cad_server` の TCP リスナーバインド)

#### 現状の実装と不具合の原因
1. `core_bridge.py` のポート番号が `8080` 固定になっている。
2. 2つ目のBlenderを起動した際、`start_server()` 内の `if _is_port_open(): return True`（580行目）が成立し、2つ目のBlenderは新しくサーバーを起動せず、1つ目のBlenderが立ち上げた `cad_server.exe`（ポート8080）に相乗りして通信を開始する。
3. しかし、`cad_server.exe` は内部で単一のジオメトリスタック空間（`occ_core.cpp`）と単一の共有メモリファイル（`seamless_cad_shm`）を前提としているため、2つのBlenderから別々のモデルデータが送信されると、スタックIDやポインタ、メッシュキャッシュが相互に上書き・衝突し、2つ目のBlender（または両方）でプレビューが正しく更新されなくなる。

#### 改修に向けた設計方針
- **マルチインスタンス対応（動的ポート割り当て）:**
  1. `core_bridge.py` に空きポート探索関数 `find_free_port(start_port=8080, max_attempts=20)` を実装し、利用可能なローカルポートを動的に取得する。
  2. 共有メモリファイル名にBlenderのPIDを付与（例: `seamless_cad_shm_{PID}`）。
  3. `cad_server.exe` のコマンドライン引数として `--port <PORT>` と `--shm <SHM_NAME>` を受け取れるよう Rust 側（`main.rs`）を拡張。
  4. 各Blenderプロセスが自身専用の `cad_server.exe` を起動・通信することで、複数Blenderの完全並行利用を実現する。

