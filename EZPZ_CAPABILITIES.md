# EZPZ (KittyCAD GCS Engine) 技術仕様・網羅的機能リファレンス

EZPZは、KittyCADがRustでスクラッチから開発した、安全・高速でモダンな **2次元幾何拘束ソルバー（GCS: Geometric Constraint Solver）** です。
本ドキュメントでは、EZPZが数学的・技術的に「何を行えるエンジンなのか」を要素、拘束、アルゴリズム、CAD統合の観点から網羅的に解説します。

---

## 1. EZPZの動作原理（数学的モデル）

EZPZは、描画やメッシュ生成を行うグラフィックスライブラリではなく、**「幾何学の関係性を連立非線形方程式として解く数値計算エンジン」** です。

```
[UI操作（ドラッグ/数値入力）]
         │
         ▼
[要素の座標を変数(x, y)として定義]
         │
         ▼
[拘束条件（平行、等長など）を代数方程式に変換]
         │
         ▼
[ヤコビ行列（Jacobian）を用いた非線形反復数値解法 (Newton-Raphson等)]
         │
         ▼
[すべての拘束を満たす「最適解」の超高速算出（ミリ秒以下）]
```

ユーザーが画面上の点をドラッグして移動させると、EZPZはその「移動後のマウス座標」を初期値（Initial Guess）とし、設定されたすべての拘束方程式を満たしつつ、元の形状から最も移動量が少ない「最小エネルギー解（局所解）」をミリ秒単位で再計算します。

---

## 2. 定義可能な幾何エンティティ（要素）

EZPZは、2Dのスケッチ平面上に以下の幾何学的な要素を「解くべき変数」として定義・配置できます。

| 要素名 | 内部的な数学的定義 | 主なユースケース |
| :--- | :--- | :--- |
| **点 (Point2D)** | 独立した変数ペア `(x, y)` | すべての幾何の基礎（端点、中心点、制御点） |
| **線分 (LineSegment2D)** | 始点 `P1(x1, y1)` と終点 `P2(x2, y2)` | 輪郭の直線部、基準軸、中心線 |
| **直線 (Line2D)** | 通過点 `P(x, y)` と 方向ベクトル `V(dx, dy)` | 対称軸、無限基準線 |
| **円 (Circle2D)** | 中心点 `C(xc, yc)` と 半径変数 `R` | ボルト穴、シャフト、ボス断面 |
| **円弧 (Arc2D)** | 中心 `C`, 半径 `R`, 開始角 `θ1`, 終了角 `θ2` | 角丸、長穴 of 端面、カム形状 |
| **楕円 (Ellipse2D)** | 中心 `C`, 長軸 `A`, 短軸 `B`, 回転角 `φ` | 特殊な意匠面、スロープの断面 |
| **制御点 (SplineControlPoint)** | 独立した変数 `(x, y)`（Splineに紐づく） | 自由曲線（NURBS/BSpline）の形状変形 |

---

## 3. サポート可能な幾何拘束（Constraints）

幾何要素同士に設定できる「ルール（方程式）」の一覧です。EZPZはこれらを自在に組み合わせて連立させます。

### ① 位置・方向に関する拘束
*   **固定 (Fixed):**
    *   特定の「点」の座標を `(X_fixed, Y_fixed)` にロックします。スケッチの基準となる「アンカー」の役割を果たします。
*   **水平 (Horizontal) / 垂直 (Vertical):**
    *   線分の `y1 = y2` または `x1 = x2` を強制します。
*   **平行 (Parallel):**
    *   2本の線分の傾き（方向ベクトル）を一致させます。
*   **直交 (Perpendicular):**
    *   2本の線分のなす角が $90^\circ$（内積が0）になるように制限します。

### ② 接続・配置に関する拘束
*   **一致 (Coincident):**
    *   点と点、または点と線を同じ座標に重ねます（線の接続や閉ループの形成）。
*   **線上配置 (Point-on-Line):**
    *   点 $P$ が、無限直線 $L$ の方程式を満たす（直線上にスライド可能）ように制限します。
*   **円周・円弧上配置 (Point-on-Circle / Arc):**
    *   点 $P$ と中心 $C$ の距離が、常に半径 $R$ と等しくなるように制限します。
*   **中点 (Midpoint):**
    *   点 $M$ の座標が、線分 $AB$ の中間点 $\frac{A+B}{2}$ と等しくなるように制限します。
*   **正接 (Tangent / 接線):**
    *   「円（または円弧）と直線」が1点のみで接するように制限します。また、「円と円」が外接・内接する状態も定義できます。

### ③ 寸法・数値に関する拘束
*   **長さ固定 (Length):**
    *   線分の長さ（始点と終点の距離）を固定値 $L$ にします。
*   **距離固定 (Distance):**
    *   任意の「2点間の距離」や「点と直線の距離」を指定の寸法 $D$ に固定します。
*   **角度固定 (Angle):**
    *   2本の直線がなす交角を指定の角度 $\theta$ に固定します。
*   **半径/直径固定 (Radius / Diameter):**
    *   円または円弧のサイズを指定の寸法にロックします。
*   **等長/等半径 (Equal Length / Equal Radius):**
    *   「線分Aと線分Bの長さを同じにする」「円Aと円Bの半径を同じにする」といった同期拘束です。

---

## 4. 高度な分析・ロジック機能

EZPZは単に方程式を解くだけでなく、CADとしての実用性を支える強力な分析機能を内部で提供可能です。

### ① 自由度（DOF: Degrees of Freedom）解析
スケッチ内のすべての要素数と拘束方程式の数から、スケッチの状態をヤコビ行列のランク解析によってリアルタイムで割り出せます。
*   **アンダー拘束 (Under-constrained):** 自由度が残っており、ドラッグで形状を変更可能。
*   **完全拘束 (Fully-constrained):** すべての寸法や位置が決まり、形状が完全にロックされた理想的な状態。
*   **過拘束 (Over-constrained):** 矛盾する拘束や冗長な拘束が追加されており、数学的に解が存在しない状態。

### ② 自動過拘束検知（矛盾の回避）
ユーザーが新しい拘束を追加しようとした際、ソルバーが「過拘束」を検知すると、その拘束の適用を自動的に拒否して直前の健全な状態にロールバック（取り消し）し、スケッチの崩壊を防ぎます。

### ③ 変数バインディング（数式パラメータテーブル）
寸法拘束の値に固定値（例: `30`）を代入するだけでなく、「他の変数を参照する数式」をバインディングできます。
*   例: `Distance_A = 50.0`
*   例: `Distance_B = Distance_A * 0.5`
これにより、ユーザーが `Distance_A` を変更するだけで、スケッチ全体の寸法が調和したまま伸縮する「パラメータ駆動設計」を実現します。

---

## 5. 3D CAD（OpenCascade）との協調設計

EZPZとOpenCascadeを組み合わせることで、以下の3D高度モデリングワークフローが実現します。

```
[1. 2Dスケッチでの拘束解決] (EZPZ)
       │ (高精度なu, v座標の算出)
       ▼
[2. 3D空間への投影・座標変換] (変換マトリクス)
       │ (任意の3D面や平面へ配置)
       ▼
[3. 3Dソリッド形状の構築] (OpenCascade)
       │ (面張り、押し出し、ロフト、アセンブリ)
```

1.  **任意の3D面へのスケッチ（ローカル座標系マップ）:**
    任意の斜め面などのトランスフォームマトリクスを用意し、3Dのクリック位置をローカル2Dに逆変換してEZPZに入力。解決後に3Dに戻すことで、複雑な多面体への穴あけ加工などが数学的破綻なしで行えます。
2.  **歴史的フィーチャーとの双方向連動（ヒストリカル・パラメトリック）:**
    「スケッチの寸法を変更する」➔「EZPZが解決」➔「OpenCascadeが押し出しやフィレットを再適用する」というツリー再計算により、手戻りの極めて少ないプロフェッショナルな設計パイプラインを構築します。
