# 実装ロードマップ

作成 2026-08-17（8.1.5.7 時点）。

`COMPETITIVE_GAP_ANALYSIS.md` の比較結果から、**実装しないもの・届かないものを落として**、
着手できる形に落とし込んだもの。到達不能な項目の議論は元文書に置いてある。

**全項目、着手前にソースを実地確認済み**（下記「棚卸しの訂正」参照）。

---

## 棚卸しの訂正 — 既存ドキュメントが古い

ロードマップを組む過程で、`FEATURE_GAPS.md` と `INVENTORY_V8_1_5_1.md` が
現在のソースに追いついていない箇所が見つかった。**先にこれを直さないと、
完成済みの機能を計画に入れることになる。**

| ドキュメントの記述 | 実際 |
|---|---|
| FEATURE_GAPS #5「計測がない」 | **完成済み。** [`ui_main_panel.py:118`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/ui/ui_main_panel.py:118) に `SEAMLESS_PT_MeasurePanel`。Part 全体（体積・表面積・寸法・重心）と、辺/面の個別計測（長さ・面積・半径）の両方。lineage が現在の形状に一致しないときは値を出さずに再選択を促す作りまで入っている |
| FEATURE_GAPS #4「エクスポート側にスケール指定が無い」 | **解消済み。** [`occ_step.cpp:121`](Blender_CAD_V_8_1_5_1/src_rust/src/occ_step.cpp:121) の `export_stack_to_step` が `scale` を取り、形状側に `gp_GTrsf` を掛けている |
| INVENTORY「全11パネル」 | **12パネル。** Measure が増えている |

FEATURE_GAPS #4 の残りの半分（名前・色・アセンブリ構造）は**未着手のまま**で、
これは下記フェーズ1に入っている。

### 未配線であることを確認した API（全て 0 ヒット）

`src_rust/src` 以下を実測。以下は「あると思って書いた」ものではない。

```
BRepAlgoAPI_Splitter   STEPCAFControl/XCAFDoc   IGESControl   StlAPI/RWStl
HLRBRep   BRepProj_Projection   MakeFilling/GeomPlate   ChFi3d/Law_*（可変フィレット）
```

Python 側も同様に確認：Hole フィーチャー無し、ユーザーパラメータ（式）無し、
フィーチャー抑制（Suppress）無し、恒久的な参照ジオメトリ無し。
（`select_reference_plane` はスケッチ平面の選択であって、ツリーに残る作業平面ではない）

---

## カーネル機能を1つ足すときの「型」

`export_stack_to_step` を追った結果。**新しいカーネル機能はすべてこの6箇所を通る。**
フェーズ1以降の見積もりはこの型を前提にしている。

| # | 場所 | 内容 |
|---|---|---|
| 1 | `src_rust/src/occ_*.cpp` | OCCT を呼ぶ本体 |
| 2 | `src_rust/src/occ_*.hpp` | 宣言 |
| 3 | `src_rust/src/api/*.rs` | 安全ラッパ。**スタックのロックを取るのはここ** |
| 4 | `src_rust/src/main.rs` | TCP のアクション文字列を分岐（`else if action == "..."`） |
| 5 | `CAD_8_1_5_1/core_bridge.py` | クライアント側の関数と応答パース |
| 6 | `CAD_8_1_5_1/operators/` + `ui/` | オペレータとボタン |

応答形式は `measure_stack` / `export_stack_to_step` が確立した
**成功 = `1u8` + ペイロード / 失敗 = `0u8` + 長さ + エラー文字列**に揃える。
新規に別の形を作らない。

> ビルドの注意：`cargo build` だけでは**テストが読むバイナリは変わらない**。
> `deploy.py` を通すこと（FEATURE_GAPS §2 で一度踏んでいる罠）。
> 配布 zip は必ず `package_addon.py` で作る（[[deploy-workflow]]）。

---

## フェーズ1 — 相互運用の回収

**必要な DLL はすべて配布物に同梱済み。** カーネルのビルドは要るが、
OCCT 側に新しい依存は増えない。3項目とも `occ_step.cpp` と同じ層に入るので、
**1つ目で型が決まれば2つ目・3つ目は速い。**

### 1.1 STEP に名前・色・アセンブリ構造を載せる 〔工数 中／効き目 高〕

`STEPControl_Writer` → `STEPCAFControl_Writer` への載せ替え。

- 触る場所：`occ_step.cpp` の `export_step` / `export_stack_to_step`
- 使う API：`TDocStd_Document` + `XCAFDoc_ShapeTool` + `XCAFDoc_ColorTool` + `STEPCAFControl_Writer`
- 同梱済み：`TKXCAF.dll` / `TKBinXCAF.dll` / `TKXmlXCAF.dll` / `TKDESTEP.dll`
- **渡すデータは既に Blender 側にある**：Part 名、グループ構造（`GROUP_START`/`GROUP_END`）

**なぜ最初か**：listing で「STEP 相互運用」を看板にしている以上、
名前も色も無い素の B-Rep では主張の半分しか届いていない。狙っている客層
（Engineers & Makers、CNC・金型）に直接効く唯一の項目。

**罠**：現在の `export_stack_to_step` は**スタック全体を1形状として書き出している**。
Part ごとに分けて名前を付けるには、書き出しの粒度そのものを変える必要がある。
ここが「載せ替え」で済まない部分。

**検証**：書き出した `.stp` をテキストで開いて `PRODUCT` / `NEXT_ASSEMBLY_USAGE_OCCURRENCE`
の名前を確認する。「FreeCAD で開ける」は何も証明しない（[[step-export-characteristics]]）。
回帰テストには**書いた STEP を読み直して名前が一致すること**を入れる。

### 1.2 STL 直接出力 〔工数 小／効き目 中〕

- 使う API：`StlAPI_Writer`（同梱 `TKDESTL.dll`）
- 現状：Bake → Blender メッシュ → Blender の STL エクスポータ、の往復が要る
- 効き目：**3Dプリント層に直接効く。** テッセレーション品質をベイク用設定で
  制御できるので、Blender 経由より質のいい STL が出る

**工数が小さい理由**：形状は既に `stack->current_shape` にあり、スケール処理は
`export_stack_to_step` のものをそのまま流用できる。§「型」の6箇所を一周するだけ。

### 1.3 IGES 書き出し 〔工数 小／効き目 低〕

- 使う API：`IGESControl_Writer`（同梱 `TKDEIGES.dll`）
- 1.1／1.2 と同じ層なので、**ついでに入れるなら安い**

**ただし優先度は低い。** IGES は STEP に置き換えられたレガシー形式で、
今から必要とする相手は多くない。1.1 と 1.2 を作った勢いで入るなら入れる、
という位置づけ。**単独で工数を割く価値は無い。**

> 参考：販売ページに一度「STEP & IGES 対応」と誤記した経緯がある
> （INVENTORY §9）。実装すれば誤記が正しくなる、という理由で優先しないこと。

---

## フェーズ2 — 設計が続かない穴

「1個目の形は作れるが、2個目で手が止まる」を潰す。**体験の変化が一番大きい**フェーズ。

### 2.1 スケッチのプロジェクト（既存エッジの投影） 〔工数 中〜大／効き目 最高〕

既存ソリッドの辺・面をスケッチ平面に投影し、**拘束をぶら下げられる参照**として使う。

- 使う API：`BRepProj_Projection`（法線／方向指定）
- 触る場所：`occ_sketch.cpp` + `api/sketch.rs` + `sketch/`（エンティティ管理）

**なぜ効き目が最高か**：既存の穴の中心にスケッチ円を合わせる、面の輪郭をなぞる、
といった操作が現状できない。スナップはあるが、**投影されたジオメトリを参照として
拘束に繋げられない**。パラメトリック CAD で手が止まる最頻出の原因がこれ。

**難所は投影計算ではなくライフサイクル**：
- 投影元が上流のフィーチャーなので、**上流が変わったら再投影が要る**
- 投影元が消えたときの扱い（スケッチを壊すか、投影結果を凍結するか）
- 既存の `core/semantic_targets.py` と同じ問題系。**先にそこを読むこと**

**段階的にやるなら**：
1. 投影を**一度きりの取り込み**として実装（再投影しない。編集すると切れる）
2. 上流変更時の再投影
3. 投影元消失時の警告

**1 だけでも実用価値がある。** むしろ 1 を出してユーザーの使い方を見てから
2 を設計するほうが正しい。

### 2.2 Hole フィーチャー 〔工数 中／効き目 高〕

ザグリ・皿穴・止まり穴・貫通を、1つのパラメータ付き操作にまとめる。

- 実装方針：**新しいカーネル機能ではなく、既存の `SUB` を内部で使う複合フィーチャー**
- 触る場所：`properties.py` の型 enum、`occ_primitives.cpp`（穴の形状生成）、
  `operators/`、`ui_main_panel.py` の CreatePanel
- 規格穴径の表（M3 → φ3.4 等）を持たせると一気に「CAD らしく」なる

**やらないこと**：ネジ山の実形状生成。重いだけで実用にならない。
穴＋表記で足りる（FEATURE_GAPS の判断を維持）。

**2.1 の後に置く理由**：穴は「既存形状のどこに開けるか」を指定できて初めて
使いやすい。投影が先にあると、穴の配置が拘束で決まるようになる。

### 2.3 Split（ボディ・面の分割） 〔工数 小／効き目 中〕

- 使う API：`BRepAlgoAPI_Splitter`（`BRepFeat_SplitShape` は既に使用中なので土地勘がある）
- 用途が広い：形状を平面で切る、面を分割して部分的にフィレットをかける、
  **断面表示にも流用できる**

**工数が小さいのに用途が広い。** フェーズ2で唯一「詰まったら後回しにできる」項目でもあるので、
2.1 が重いと感じたときの気分転換に置いておける。

### 2.4 参照ジオメトリ（作業平面・軸・点） 〔工数 中／効き目 中〕

ツリーに残る恒久的な平面・軸・点。ミラー、配列、スケッチ平面、配置の基準になる。

- 現状：`select_reference_plane` は**その場のスケッチ平面選択**で、ツリーに残らない
- 触る場所：`properties.py` の型 enum に追加。カーネルは形状を持たないので**Python 側が主**
- `ROADMAP_V1_4_0.md` の「参照プレーン（基準面）機能」がこれ

---

## フェーズ3 — パラメトリックの基盤

「後から編集できる」という商品の主張を、**値の編集から設計意図の編集へ**引き上げる。

### 3.1 ユーザーパラメータ（変数と数式） 〔工数 中／効き目 高〕

`width = 40`、`hole_d = width / 4` のような変数テーブルと式評価。

- **カーネルに一切触らない。Python 側で完結する。** フェーズ中で最もリスクが低い
- 触る場所：`properties.py`（変数テーブルの `PropertyGroup`、各パラメータの式文字列）、
  評価順を決める依存グラフ、`ui/` に一覧パネル
- `ROADMAP_V1_4_0.md` の「変数・寸法スプレッドシート」がこれ

**なぜ 3.2 より先か**：ツリー挿入（3.3）と並ぶ価値がありながら、
**lineage に触らない**ので独立して進められる。1つの数字を変えて設計全体が
追従する体験は、ツリー挿入と同じくらい「パラメトリックである」ことを伝える。

**罠**：式のパースに `eval()` を使わないこと。`.blend` は他人から受け取るファイルで、
その中の文字列を評価するのは任意コード実行になる。四則演算と変数参照だけの
小さなパーサを書く（あるいは AST を明示的にホワイトリストする）。

### 3.2 フィーチャーの抑制（Suppress）とリネーム 〔工数 小／効き目 中〕

- **Suppress**：ロールバックは「そこから先を全部止める」だけで、
  「この1個だけ切る」ができない。`core_bridge.py` の再計算ループで
  フラグの立った要素を飛ばすだけ。**カーネル改変なし**
- **リネーム**：地味だが、ツリーが10行を超えると効いてくる

**どちらも 3.1 の作業中に片手間で入る規模。** 独立したマイルストーンにしない。

### 3.3 Feature Tree への挿入・並べ替え 〔工数 大／効き目 最高〕

FEATURE_GAPS #1。**依然として最大の課題であり、最後に置くという判断も変えない。**

- 本体は「move する」ことではなく、**順序変更で壊れる lineage 参照を検出して
  ユーザーに見せること**。黙って壊すのが最悪
- 段階：(1) 隣接スワップのみ・壊れるなら理由付きで拒否 →
  (2) ロールバック位置への挿入 → (3) 任意位置へのドラッグ
- **(1) だけでも実用価値がある。(3) まで行かなくてよい**

**着手条件**：フェーズ1・2 と 3.1 が片付いていること。
逆に、ここに手を付ける気になったら他を止めてよい。それだけの価値がある。

---

## フェーズ4 — 表現力（余力があれば）

**優先しない。** フェーズ1〜3 が片付いていない状態でここに来ないこと。

### 4.1 可変半径フィレット 〔工数 小〜中〕

- 使う API：`BRepFilletAPI_MakeFillet` の Law 版（`Law_Linear` / `Law_S`）
- 既存のフィレット実装（[`occ_modifiers.cpp:765`](Blender_CAD_V_8_1_5_1/src_rust/src/occ_modifiers.cpp:765)）に
  半径2つ目のパラメータを足す形。**新規の型を作らなくてよい**
- `ROADMAP_V1_4_0.md` 既出。「プロっぽさ」の割に安い

> Measure パネルの実装コメントが「可変フィレットは BSpline になる」と
> 既に想定している。半径が出ないことと測れなかったことを区別する作りになっているので、
> 計測側は変更不要。

### 4.2 N辺パッチ（Fill / Patch） 〔工数 中〕

- 使う API：`BRepOffsetAPI_MakeFilling`（内部は `GeomPlate_BuildPlateSurface`）
- 境界曲線から面を張る。隣接面に対し **G0/G1/G2 拘束**を個別指定でき、点拘束も足せる
- **Plasticity 的な表現力に現実的に届く唯一の入口**

面の品質（曲率の素直さ）では Parasolid / XNURBS に及ばないが、
「張れない」から「張れる」への差のほうが大きい。

### 4.3 三面図 SVG 書き出し 〔工数 中〜大〕

- 使う API：`HLRBRep_Algo` / `HLRBRep_HLRToShape`（同梱 `TKHLR.dll`）+ `svgwrite`（同梱済み）
- 正面・平面・側面・等角の投影を隠線処理して SVG に落とす

**寸法記入・公差・注記はやらない。** FEATURE_GAPS が「沼」と呼んだのはそこであって、
投影図を吐くところまでは沼ではない。CNC・レーザー加工・板金の見積もり用途では、
寸法が入っていなくても輪郭が出るだけで実用になる。

**線引きを守ること。** 寸法記入に手を出した時点で製品1つ分の工数になる。

---

## この計画で扱わないもの

理由は `COMPETITIVE_GAP_ANALYSIS.md` §8 に置いてある。ここでは一覧だけ。

CAM／FEA／板金展開／アセンブリ拘束／図面の寸法記入／クラウド連携／
ネジ山の実形状／G2 曲率連続ブレンド／真の move face（再交差つき）／
3MF・DXF／プロプライエタリカーネルへの乗り換え

---

## 順序の要約

```
フェーズ1  1.1 STEP+XCAF → 1.2 STL → (1.3 IGES はついでに)
   ↓        同梱DLLで完結。看板の主張が埋まる
フェーズ2  2.1 投影 → 2.2 Hole → 2.3 Split → 2.4 参照ジオメトリ
   ↓        「2個目で手が止まる」の解消。体験の変化が最大
フェーズ3  3.1 変数と数式 → 3.2 Suppress/リネーム → 3.3 ツリー挿入
   ↓        パラメトリックの基盤。3.3 は他が全部片付いてから
フェーズ4  4.1 可変フィレット → 4.2 N辺パッチ → 4.3 三面図SVG
            余力があれば。ここから始めないこと
```

### 判断の根拠

- **フェーズ1が先頭なのは、書くコード量に対して主張が埋まる量が最大だから。**
  新しい外部依存がゼロで、既に配っているバイナリを使うだけ
- **2.1（投影）は順位こそ4番目だが、「CAD として使えるか」に一番効く穴。**
  フェーズ1が軽いので、実質すぐ着手できる位置にある
- **3.1（変数）を 3.3（ツリー挿入）より先に置いたのが、既存ロードマップからの
  実質的な変更点。** 効き目が近いのに lineage に触らない分リスクが低い
- **フェーズ4 は全部「無くても製品として成立する」もの。** ここに早く来ないこと

### 着手前に必ずやること

`FEATURE_GAPS.md` #5 を「完了」に、#4 のスケール指定の記述を修正、
`INVENTORY_V8_1_5_1.md` のパネル数を 12 に更新する。
**古い棚卸しを見ながら計画を進めると、完成済みの機能をもう一度作ることになる。**
