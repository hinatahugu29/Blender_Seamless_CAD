# Seamless CAD V1.2.1 実装計画: モディファイア・システム刷新

## 目的
ミラー、アレイ、サークル等のモディファイアにおいて、「セット全体を動かす」操作性と「不要な元形状の非表示」を実現し、CADとしての使い勝手を飛躍的に向上させる。

## 1. ターゲットの「所有」と親子関係の自動化
モディファイアがターゲットを選択した際、Blenderの親子関係（Parenting）を自動的に構築する。

- [ ] `modal_selection.py` または `operators.py` にて、ターゲット選択確定時に `child.parent = modifier` を実行。
- [ ] 座標を維持したまま親子付けを行うための `matrix_parent_inverse` の適切な設定。
- [ ] ターゲットが解除された際の親子関係の解消（Unparenting）。

## 2. 二重表示の解消（ターゲットのステルス化）
ターゲットにされたオブジェクトが個別にレンダリングされるのを防ぎ、モディファイアの一部としてのみ表示されるようにする。

- [ ] `core_bridge.py` にて、他のプリミティブの `target_uuid` に含まれているオブジェクトをリストアップ。
- [ ] レンダリング・ループ（`drawing.py` またはプロキシ生成ロジック）にて、ターゲットに指定されているオブジェクトの描画をスキップするフラグを導入。

## 3. モディファイア軸・中心の独立制御
全体の移動（Parent移動）と、パターン内部の間隔（Local Offset/Plane）を分離する。

- [ ] **Mirror**: 
    - ミラーオブジェクトの原点を「鏡の面」として扱う。
    - ターゲットの位置はミラーオブジェクトからの相対位置で決定。
- [ ] **Array / Circular Array**:
    - モディファイアの原点を「パターンの起点/中心」とする。
    - ターゲットの相対的なズレを維持したまま複製。
- [ ] **Revolution**:
    - 回転軸をモディファイアのZ軸に固定。

## 4. C++コア側の座標系アップデート
世界座標ではなく、モディファイアのローカル座標系に基づいた計算へのシフト。

- [ ] `occ_core.cpp` 内のパターン計算ロジック（Mirror, Array等）において、入力形状の座標をモディファイアの座標系に引き込んでから演算を行うように調整。

---

## 作業フェーズ
1. **Phase 1: UI/Blender連携 (Parenting)** - オブジェクトが一緒に動く状態を作る。
2. **Phase 2: 表示制御 (Stealth Mode)** - 重なって表示される不自然さを消す。
3. **Phase 3: Coreロジック修正** - 移動しても間隔が壊れないように計算式を微調整。
4. **Phase 4: テスト & 安定化** - 各種パターンの動作確認。
