# V1.3.2 検討議題: "Topological Alignment & Attachment"

> [!NOTE]
> **ミッション**
> グローバル座標系での配置から、幾何構造（面・辺・点）に基づいた「相対配置」へのシフト。
> V1.3.1 で確立したキャッシュ基盤を活かし、形状変更に追従する動的な配置システムを構築する。

---

## 1. 面基準の整列機能（Face Alignment）
ユーザーが選択した「面」に対して、オブジェクトを一撃でフィットさせる機能。

- **Face-Normal Snap**: 選択した面の中心座標と法線方向を取得し、プリミティブの Location/Rotation を自動設定。
- **UI 案**: 3Dビュー上で面を選択し「Align to Face」ボタンを押すことで、アクティブなオブジェクトが吸着。

## 2. トポロジー・アタッチ（UUID 拘束）
単なる配置ではなく、ベース形状の変更に追従する親子関係。

- **Face-Parenting**: プリミティブに `target_face_uuid` を保持させる。
- **Dynamic Update**: ベース形状の計算が終わるたびに、指定された UUID の面を探し、その現在の位置に子プリミティブを再配置する。
- **期待される効果**: 「下の箱の長さを変えると、上に開けた穴も自動でついてくる」という CAD 的な非破壊編集の完成。

## 3. スケッチ・パイプラインの布石
将来的な「面に描く」機能への準備。

- **Local Coordinate System (LCS)**: 面の上に独自の 2D 座標系（Origin, X, Y）を定義するロジックの開発。
- **Projection**: Blender 上で描いた 2D 曲線（Bezier/Mesh）を、LCS を介して 3D 空間の面の上に投影する技術検証。

## 4. 適応型品質制御 (Adaptive Quality Control) [DONE]
ビューポートの快適さと、最終出力の精密さを両立するパラメトリックな精度管理。

- **Adaptive Tessellation**: `BRepMesh_IncrementalMesh` の deflection 値を UI から制御。
- **LOD Preview**: ドラッグ中の高速プレビュー（低精度）と、静止時の精密描画を自動切り替え。
- **High-Quality Bake**: ベイク時のみ、ユーザーが指定した高精度設定で再計算を実行するロジック。

---

## 📅 進捗状況

- [x] **適応型品質制御**: Viewport/Bake 品質スライダーの実装とコアへの伝達 [2026-05-13]
- [ ] **フェーズ A**: 面の選択 UI と、法線・中心取得ロジックの Rust 側実装。
- [ ] **フェーズ B**: UUID を介した「再計算時の位置自動同期」のテスト。
- [ ] **フェーズ C**: スケッチデータの OCCT 押し出し命令への変換テスト。

---

> [!TIP]
> **V1.3.2 が目指す体験**
> 「座標値を手入力する」モデリングから、「この面にこれを置く」という直感的な CAD ワークフローへの進化。
