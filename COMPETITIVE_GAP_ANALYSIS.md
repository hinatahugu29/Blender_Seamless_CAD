# 他社CADとの機能比較 — 何が無く、何が届き、何を諦めるか

作成 2026-08-17（8.1.5.7 時点）。

比較対象は **Fusion 360**（パラメトリック・ヒストリベース／機械系）と
**Plasticity**（Parasolid・ダイレクトモデリング／コンセプト・工業デザイン系）。
この2つで CAD の主要な流派をほぼ覆える。

`FEATURE_GAPS.md` は「自分のコードを読んで見つけた穴」だった。
本書は **「外から見て、他社にあってこれに無いもの」** を起点にしている。
役割が違うので両方残す。重複する項目は本書側で FEATURE_GAPS の番号を参照する。

判定は4段階：

| 判定 | 意味 |
|---|---|
| **A** | 必要なバイナリは**すでに配布物に入っている**。書くのはコードだけ |
| **B** | OCCT に API はある。実装が要る |
| **C** | OCCT に無い。自作するか、外部ライブラリを足す |
| **D** | カーネル差または別製品。**諦める** |

---

## 0. 先に — 一番安い山はここにある

配布 ZIP には OCCT のツールキットが **74個** 入っている。そのうち、
**機能として配線されていないもの**を洗い出した結果：

| 同梱済み DLL | 何ができるか | 現状 |
|---|---|---|
| `TKXCAF` / `TKBinXCAF` / `TKXmlXCAF` | STEP に**名前・色・アセンブリ構造**を載せる | 未使用（`STEPControl_Writer` 直叩き） |
| `TKDEIGES` | **IGES 読み書き** | 未使用。販売ページで一度誤記したあの機能 |
| `TKDESTL` | **STL 直接出力**（Bake を経由しない） | 未使用 |
| `TKDEGLTF` / `TKDEOBJ` / `TKDEPLY` / `TKDEVRML` | 各種メッシュ形式の入出力 | 未使用 |
| `TKHLR` | **隠線処理された投影図の生成**（＝2D図面の心臓部） | 未使用 |
| `TKFeat` | `BRepFeat_*`（面へのローカル操作） | `SplitShape` のみ使用 |

さらにカーネル側には **`measure_stack()` が完全に実装済み**で、体積・表面積・
重心・最適バウンディングボックスを返す（[`occ_core.cpp:2041`](Blender_CAD_V_8_1_5_1/src_rust/src/occ_core.cpp:2041)、
[`core_bridge.py:1083`](Blender_CAD_V_8_1_5_1/CAD_8_1_5_1/core_bridge.py:1083) まで配線済み）。
**呼んでいるのは回帰テストだけで、UI が無い。**

> つまり「計測機能を作る」の実体は、カーネルにもブリッジにも手を入れず
> **パネルを1枚足すこと**。FEATURE_GAPS #5 の見積もりより、さらに安い。

一方で、`TKIVtk` / `TKOpenGl*` / `TK*Test` / `TKDraw` / `TKQADraw` /
`TKXSDRAW*` / `TKViewerTest` は OCCT の可視化・デバッグ用で、依存の VTK も Qt も
外してあるため**ロードすらできない死荷重**。ここは逆に削れる（別件）。

---

## 1. スケッチ（2D）

Plasticity は拘束スケッチャを持たない。**この領域は既に Plasticity に勝っている。**
比較対象は Fusion のみ。

| Fusion にあるもの | 本アドオン | 判定 | 備考 |
|---|---|---|---|
| 幾何・寸法拘束 | 14種（8.1.5.6 で5種追加） | — | 実用域。`ezpz` に未使用の拘束がまだ9種ある |
| トリム／延長／オフセット／フィレット | あり | — | |
| **プロジェクト（3Dエッジをスケッチ平面に投影）** | **無し** | **B** | ← **最大の穴**。下記 |
| **楕円** | 無し | **B** | `gp_Elips`。安い |
| **スケッチ内の矩形／円形パターン** | 無し | **B** | 点と拘束を複製するだけ。中 |
| **スケッチ文字（Text）** | 無し | **C** | Blender のフォント→カーブ経由で代替可 |
| **駆動寸法／参照寸法の区別** | 無し | **B** | 表示と DoF 計上の分離だけ。安い |
| **寸法線の作図**（引出線・矢印） | 数値ラベルのみ | **B** | 2D_CAD_ROADMAP フェーズ5.1 が未達 |
| **拘束バッジ表示** | 無し | **B** | 同フェーズ4.2 が未達 |
| スプライン（制御点／フィット点） | Curve/BSpline あり | — | |

### プロジェクト（投影）が最大の穴である理由

既存ソリッドの穴の中心にスケッチ円を合わせる、面の輪郭をなぞって別形状を作る——
**2つ目以降のフィーチャーを既存形状に「合わせて」描く手段が、現状ほぼ無い。**
スナップはあるが、投影された参照ジオメトリとして拘束をぶら下げることができない。

パラメトリック CAD で「最初の1個は作れるが、2個目から手が止まる」典型的な原因がこれ。
OCCT 側は `BRepProj_Projection`（法線／方向指定の投影）で、面や辺をスケッチ平面に
落とせる。難所は投影結果を**スケッチ側のエンティティとして持ち、上流が変わったら
再投影する**というライフサイクル管理のほう。既存の semantic_targets と同じ問題系。

---

## 2. 3Dフィーチャー

| 他社にあるもの | 本アドオン | 判定 | 備考 |
|---|---|---|---|
| 押し出し・回転・スイープ・ロフト | あり（34型） | — | 品揃えは価格帯に対して十分 |
| **穴（ザグリ・皿・止まり・貫通）** | 円柱を SUB | **B** | FEATURE_GAPS #3。既存 SUB の複合操作 |
| **押し出しの「次まで／全貫通」** | 距離指定のみ | **B** | 当たり判定を自前で。中 |
| **可変半径フィレット** | 定数半径のみ | **B** | `BRepFilletAPI_MakeFillet` の Law 版。V1.4.0 ロードマップ既出 |
| **面フィレット（面と面の間を埋める）** | 無し | **C/D** | OCCT は辺ベース。ここは弱い |
| **ボディ／面の分割（Split）** | 無し | **B** | `BRepAlgoAPI_Splitter`。**安いのに効果が大きい**。断面表示にも流用可 |
| **リブ／ウェブ** | 無し | **B** | |
| **ねじ（規格表つき）** | 無し | **B** | 表記のみ。実形状は作らない（FEATURE_GAPS の判断を支持） |
| 抜き勾配・シェル | あり | — | `DraftAngle` / `MakeThickSolid` 使用中 |
| **参照ジオメトリ（作業平面・軸・点）** | スケッチ平面選択のみ | **B** | V1.4.0 ロードマップ既出。ミラー・配置の基準として効く |
| **フィーチャーの抑制（Suppress）** | ロールバックのみ | **B** | 安い。「この1個だけ切る」ができない |

### 面の押し引きについて（前回の補正）

前回「押し引きは未実装」と述べたが、**`FACE_OFFSET` として既に動いている**。
実装は [`occ_modifiers.cpp:1182`](Blender_CAD_V_8_1_5_1/src_rust/src/occ_modifiers.cpp:1182)：
対象面を lineage で解決 → 法線を求め `BRepClass3d_SolidClassifier` で内外を確定 →
`BRepPrimAPI_MakePrism` で押し出し → 正負に応じて Fuse / Cut。
生成された TOP/SIDE 面は face_map に登録される。

ただしこれは **「面の上にボスを生やす／掘る」** であって、Parasolid の
`PK_FACE_transform` のような **「面を動かして隣接面を追従させる」** ではない。
面が形状の端まで届いていない場合、境界に段差が残る。真の move face は判定 **D** のまま。

---

## 3. ダイレクトモデリング・サーフェス（Plasticity の主戦場）

| Plasticity にあるもの | 本アドオン | 判定 | 備考 |
|---|---|---|---|
| **G2 曲率連続ブレンド** | 無し | **D** | Parasolid のブレンド品質。OCCT では届かない |
| フィレットの堅牢性 | `BRepFilletAPI` | **D** | OCCT で最も苦情の多い部分。改善はできても並ばない |
| **N辺パッチ（Patch / Fill）** | 無し | **B** | `BRepOffsetAPI_MakeFilling`。G0/G1/G2 拘束＋点拘束が可能。**ここは届く** |
| Coons パッチ（2〜4曲線） | 無し | **B** | `GeomFill_BSplineCurves`。安い |
| サーフェスの縫合（Sew） | 無し | **B** | `BRepBuilderAPI_Sewing` |
| サーフェスのトリム／アントリム | 無し | **C** | |
| **真の move face（再交差つき）** | 無し | **D** | §2 参照 |
| **面の削除＋治癒** | 無し | **B** | `BRepAlgoAPI_Defeaturing`（OCCT 7.4+） |
| ヒストリを持たない設計 | ヒストリベース | — | **これはカーネル差ではなく製品判断**。真似する必要はない |

### この領域の判断

Plasticity の「気持ちよさ」の正体を分解すると、**カーネル由来（ブレンド品質・
再交差）はほぼ D、機能の存在自体（パッチ・フィル）は B** に分かれる。
つまり**サーフェス表現力は取りに行けるが、面品質での勝負は避けるべき**。

そして Plasticity は公式の Blender ブリッジを持つが、それは**往復のワークフロー**である。
このアドオンは Blender の中にいる。**往復が要らないこと自体が競合優位**であり、
面品質で正面から殴り合う必要はない。

---

## 4. パラメトリックの基盤

ここが Fusion との差で一番「格が違って見える」ところ。

| Fusion にあるもの | 本アドオン | 判定 | 備考 |
|---|---|---|---|
| **ユーザーパラメータ（変数と数式）** | 無し | **C** | `width = 40`, `hole = width/4` のような式。**中コスト・最高価値** |
| **ツリーの途中に挿入／並べ替え** | 末尾追加のみ | **C** | FEATURE_GAPS #1。lineage の設計と正面衝突 |
| **依存関係の可視化** | 無し | **C** | 「これを消すと何が壊れるか」 |
| ロールバック | あり | — | |
| **フィーチャーのリネーム** | 無し | **B** | 地味に効く。安い |
| コンフィギュレーション／派生バリエーション | 無し | **D** | 別製品規模 |

### 変数・数式について

`ROADMAP_V1_4_0.md` の「変数・寸法スプレッドシート」がこれ。
**実装は純粋に Python 側で完結する**（カーネルに一切触らない）。
`PropertyGroup` に変数テーブルを持ち、各パラメータに式文字列を持たせ、
評価順を依存グラフで決めて数値を書き戻すだけ。
Blender の driver に載せる手もあるが、CAD 的な変数表として見せるほうが伝わる。

**FEATURE_GAPS #1（ツリー挿入）よりリスクが低く、体験の変化は同等以上**かもしれない。
「後から編集できる」という商品の主張に、別の角度から届く。

---

## 5. 相互運用

| 形式 | Fusion | 本アドオン | 判定 |
|---|---|---|---|
| STEP 読み | ✅ | ✅ | — |
| STEP 書き（幾何のみ） | ✅ | ✅ | — |
| **STEP 書き（名前・色・アセンブリ）** | ✅ | ❌ | **A** — `TKXCAF` 同梱済み。FEATURE_GAPS #4 |
| **IGES** | ✅ | ❌ | **A** — `TKDEIGES` 同梱済み |
| **STL 直接** | ✅ | Bake 経由 | **A** — `TKDESTL` 同梱済み |
| **3MF** | ✅ | ❌ | **C** — OCCT に無い |
| **DXF** | ✅ | ❌ | **C** — OCCT に無い |
| SVG 読み | — | ✅ | — |
| glTF / OBJ | ✅ | Blender本体 | **A**（不要かも） |

**A が4つ並んでいるのは、この表だけ。** 出荷済みバイナリで書けるうえに、
listing の「STEP 相互運用」という看板に直接効く。

> 注意：`.stp` に色と名前が乗るだけで、受け取る側の印象がかなり変わる。
> [[step-export-characteristics]] の実測（1 Blender unit = 1 mm 固定、
> エクスポート側にスケール指定なし）も、同じ場所を触るついでに直せる。

---

## 6. 計測・検査

| Fusion にあるもの | 本アドオン | 判定 |
|---|---|---|
| **体積・表面積・重心** | カーネル実装済み・**UI 無し** | **A** |
| **バウンディングボックス** | 同上（AddOptimal で実測済み） | **A** |
| **2点間／辺／面の距離** | `BRepExtrema_DistShapeShape` 使用中（ピッキング） | **B**（UI のみ） |
| **角度計測** | 無し | **B** |
| **断面解析（セクション表示）** | 無し | **B** — `BRepAlgoAPI_Section` |
| **干渉チェック** | 無し | **B** — INT の体積を見るだけ |
| 勾配解析・曲率解析（ゼブラ等） | 無し | **C** |

**3Dプリント層は体積（＝材料費）を知りたがる。** そして今、その数字は
カーネルの中に既にある。パネル1枚。

---

## 7. 2D図面

Fusion の中核機能のひとつ。本アドオンには無い。
`FEATURE_GAPS.md` は「沼。単体で製品1つ分の工数」として却下していて、
**寸法記入まで含めればその判断は正しい。**

ただし分解すると：

| 段階 | 内容 | 判定 |
|---|---|---|
| 1 | 隠線処理された投影図（正面・平面・側面・等角）を生成 | **B** — `TKHLR` 同梱済み |
| 2 | SVG として書き出す | **A** — `svgwrite` 同梱済み |
| 3 | 図面枠・表題欄 | **B** |
| 4 | 寸法記入・公差・注記 | **D**（沼） |

**1〜2 だけなら「ワンボタンで三面図 SVG を吐く」が成立する。**
CNC・レーザー加工・板金の見積もり用途では、寸法が入っていなくても
輪郭の SVG/DXF が出るだけで実用になることが多い。
沼なのは 4 であって、1〜2 は沼ではない。**却下の粒度を見直す価値がある。**

---

## 8. 確実に無理・諦めるべきもの

理由を明示して、以後迷わないための一覧。

| 項目 | なぜ諦めるか |
|---|---|
| **Parasolid / ACIS / CGM への乗り換え** | GPL-2.0-or-later ではリンク不可。**金額以前にライセンスで閉じている**。OCCT が唯一の選択肢 |
| **G2 曲率連続ブレンド／面フィレット** | カーネル固有の品質差。OCCT では原理的に届かない |
| **真の move face（再交差・トポロジ変化つき）** | 同上。平面・円筒に限定した劣化版なら B だが、汎用は D |
| **CAM（工具経路生成）** | 別製品。専業がひしめく市場 |
| **FEA / シミュレーション** | 別製品。OCCT にソルバは無い |
| **シートメタル（板金展開）** | OCCT に unfold 機能なし。曲げ表・K係数・展開計算をすべて自作。工数が製品1つ分 |
| **アセンブリ拘束（メイト）** | FEATURE_GAPS の判断を支持。Part + Instance + Blender の親子で当面足りる |
| **図面の寸法記入・公差** | §7 の段階4。ここだけが沼 |
| **クラウド／版管理／コラボ** | Fusion の事業モデルそのもの。Blender の .blend で足りる |
| **ネジ山の実形状生成** | 重いだけで実用にならない（FEATURE_GAPS 済） |
| **レンダリング** | **Blender 本体がやる。これは穴ではなく、むしろ本アドオンの強み** |

---

## 9. ロードマップ素案

`FEATURE_GAPS.md` の推奨順（2→5→3→4→1）に、本書の発見を合流させたもの。
**判定 A を先頭に寄せている**のが唯一の実質的な変更。

### 第0段：出荷済みバイナリの回収（すべて判定 A）

コストに対して見返りが最も大きい。カーネルのビルドすら要らないものが混ざる。

1. **計測パネル** — `measure_stack` を UI に出すだけ。カーネル改変ゼロ
2. **STEP に名前・色・アセンブリ** — `STEPCAFControl_Writer` へ載せ替え。看板の残り半分
3. **STL 直接出力 / IGES** — 同梱 DLL の配線

### 第1段：設計体験の穴（B、費用対効果が高い順）

4. **スケッチのプロジェクト（投影）** — 「2個目のフィーチャーで手が止まる」の解消
5. **穴（Hole）専用フィーチャー** — 機械系の作業量が体感で変わる
6. **ボディ／面の分割（Splitter）** — 安いのに用途が広い（断面表示にも流用）
7. **参照ジオメトリ（作業平面・軸・点）**

### 第2段：パラメトリックの底上げ

8. **ユーザーパラメータ（変数と数式）** — Python 側のみで完結。商品の主張に直結
9. **フィーチャーの抑制・リネーム** — 安い
10. **ツリーへの挿入／並べ替え**（FEATURE_GAPS #1）— lineage と正面から向き合う

### 第3段：表現力の拡張（余力があれば）

11. **N辺パッチ（MakeFilling）** — Plasticity 領域に届く唯一の現実的な入口
12. **可変半径フィレット**
13. **三面図 SVG 書き出し（HLR）** — 寸法記入はやらない

### 順序の考え方

- 第0段は**他のどれよりも先**。書くコードの量に対して、listing の主張が埋まる量が一番大きい
- 第1段の4（投影）は、順位こそ4番目だが**「CAD として使えるか」に一番効く**穴
- 第2段の10（ツリー挿入）は依然として最大の課題。**他を全部片付けてから腰を据える**という
  FEATURE_GAPS の判断は変えない
- 第3段は「Plasticity と戦う」ためではなく「表現の天井を上げる」ため。優先しない

---

## 付記：この比較で見えた立ち位置

- **スケッチ拘束**：Plasticity に勝っている。Fusion には劣るが差は縮められる
- **モデリング操作の品揃え**：34型。この価格帯で不足していない
- **面品質・ブレンド堅牢性**：Plasticity に負ける。**カーネル差なので受け入れる**
- **パラメトリック基盤**：Fusion に負ける。**ここは自力で縮められる**
- **Blender 統合**：他社が持っていない。往復が要らない。**唯一の構造的優位**

競合が「CAD の隣に Blender を置く」構成なのに対し、こちらは
**Blender の中に CAD がある**。比較表で勝つ必要はなく、
往復が消えることの価値を削らない方向に投資すべき、というのが本書の結論。
