# Blender CAD アドオン ソースコード統計 (V8.1.5時点)

本アドオン（Blender_CAD V8.1.5）を構成する独自ソースコードのファイル数および行数（LOC: Lines of Code）の集計データです。
※同梱されている外部依存ライブラリ（NumPy、SciPy等）や自動ビルド成果物は除外しています。

---

## 1. 言語別集計

| 言語 | ファイル数 | 行数 (LOC) | 構成比 (行数) | 主な役割・機能範囲 |
| :--- | :---: | :---: | :---: | :--- |
| **Python** | 53 | **18,473** | 60.5% | BlenderのUI（サイドバー・設定）、操作モーダル（スケッチステート等）、履歴管理、プロキシ同期 |
| **C++** | 26 | **7,336** | 24.0% | OpenCASCADE (OCCT) カーネルと直接通信する高速ブリッジ、CADサーバーのコアロジック |
| **Rust** | 20 | **4,745** | 15.5% | プレビュー用BSP（境界表現）エンジン、シェーダー管理、高速描画データの転送 |
| **合計** | **99** | **30,554** | **100%** | **Blender CAD アドオンの総開発規模** |

---

## 2. 特徴とアーキテクチャ設計

本アドオンは、ユーザーインターフェースやBlenderとの統合部分を軽快な **Python** で構築し、計算負荷の高いCADカーネルの制御やリアルタイムプレビューを **C++ (OCCT)** および **Rust** に委ねる三層のハイブリッド構造を採用しています。

1. **Python 層 (フロントエンド / 約 18,500行)**
   - `properties.py` や `ui_main_panel.py` などのUI定義、および `sketch/states/` 配下のモーダル描画ツール（SlotやTrim/Extendなど）のステートマシンが大部分を占めます。
2. **C++ 層 (CADカーネル制御 / 約 7,300行)**
   - `src_rust/src/occ_core.cpp` や `occ_modifiers.cpp` などで、正確なB-rep形状表現を構築するためにOpenCASCADEのAPIを細かく叩くC++ブリッジが記述されています。
3. **Rust 層 (描画 & ユーティリティ / 約 4,700行)**
   - 頂点スナップや高速なライブプレビュー描画を行うBSPエンジンの制御が `lib.rs` や `main.rs` などで安全かつ高速に実装されています。
