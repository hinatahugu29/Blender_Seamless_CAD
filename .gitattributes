# このリポジトリのファイルは LF / CRLF が混在している
# (core_bridge.py と utils.py は LF、drawing.py と __init__.py は CRLF など)。
# core.autocrlf=true が有効なため、放置すると checkout のたびに改行が書き換わり、
# 中身を変えていないファイルが差分として大量に出てくる。
#
# `-text` で改行変換を完全に無効化し、現在のバイト列をそのまま保存する。
# 改行を統一したくなったら、それは独立したコミットとして行うこと。
* -text
