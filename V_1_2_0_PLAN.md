# Seamless CAD V1.2.0 実装計画書: 「Performance & Precision」

V1.1.4 までの安定化フェーズを経て、V1.2.0 では**「商用 CAD に匹敵するレスポンス」**と**「より直感的な操作体系」**の実現を目指します。本計画は、`PERFORMANCE_ROADMAP.md` の第1段階および第3段階を主要ターゲットとしています。

---

## 1. 開発コンセプト
- **Performance**: 「全件再計算」から「差分更新」へのパラダイムシフト。
- **Fluidity**: 操作中のカクつきをゼロにし、吸い付くような操作感を実現。
- **Advanced Tools**: 実用的な機械設計に必要な「厚み（Shell）」と「勾配（Draft）」の完備。

---

## 2. 主要実装項目

### ① 依存関係ベースの計算キャッシュ (Seamless Cache)
現状、パラメータを 1 つ変えるだけで全履歴を再計算していますが、これを依存グラフに基づいた部分更新に切り替えます。

- **Rust 側の変更**:
    - 各 CAD オブジェクトに `Hash` (パラメータのハッシュ) と `CachedShape` を保持。
    - 履歴ツリーを解析し、変更がないノードの計算をスキップ。
    - **期待効果**: 複雑なブーリアンモデルでの再計算時間を 50~90% 削減。

### ② インタラクティブ LOD (Draft Mode)
マウス移動中（ドラッグ中）のみ計算精度を動的に落とし、表示の遅延を解消します。

- **Python/Rust 連携**:
    - モーダル実行中（移動、サイズ変更等）は `Deflection` (テッセレーション精度) を粗く設定。
    - マウスを離した瞬間にバックグラウンド（または同期）で高精度なメッシュを生成。
    - **期待効果**: 数万ポリゴンを超えるモデルでも、ドラッグ操作が 60FPS で安定。

### ③ 高度なモデリング機能の UI 統合
コアエンジンには実装済みの機能を、Blender のプロパティパネルおよびビューポートから操作可能にします。

- **Shell (シェル化)**: オブジェクトの中空化。特定の面を除去して厚みを付ける。
- **Draft (勾配付け)**: 抜き勾配の設定。金型設計等に必須。
- **Offset (面オフセット)**: 指定した面を法線方向にオフセット。

### ④ ビューポート・ダイレクトギズモ
サイドバーの数値入力だけでなく、3D ビューポート上で形状を直接操作できるようにします。

- **実装内容**:
    - Blender Gizmo API を使用した専用ギズモの作成。
    - Box の幅/奥行き/高さ、Cylinder の半径/高さをドラッグで変更。
    - 操作中は ② の Interactive LOD と連動。

---

## 3. 技術的ロードマップ（V1.2.0 開発フェーズ）

### フェーズ 1: キャッシュ基盤の構築 [DONE]
- Rust 側の `CadEngine` 構造体を拡張し、Shape キャッシュ機能を実装。
- **備考**: パラメータハッシュによる劇的な高速化を達成。

### フェーズ 2: インタラクティブ性の向上 [DONE]
- Python モーダルのリファクタリング。`MOUSEMOVE` 時の LOD 切り替えロジックの実装。
- **備考**: ドラッグ操作の軽量化を実現。

### フェーズ 3: 高度な機能と UI [DONE]
- Shell / Draft のプロパティパネル実装。
- 3D ビューポート用ギズモとの統合。
- **備考**: 日本語ガイド付きのインタラクティブな編集環境を構築。

---

## 4. 懸念事項と対策

| 懸念事項 | 対策 |
| :--- | :--- |
| **メモリ使用量** | キャッシュする形状は `TopoDS_Shape` のみに限定し、メッシュデータは必要時以外破棄する。 |
| **トポロジーの不整合** | キャッシュ使用時も UUID (Zone Tracking) が正しく引き継がれるか厳密にテストする。 |
| **ビルドの複雑化** | `Rayon` 等の新ライブラリ導入に伴い、Windows/Mac/Linux 全環境でのビルドパイプラインを再確認する。 |

---

## 5. 次のステップ
1.  **Rust 側のキャッシュプロトタイプ作成**: パラメータ変更時の再計算スキップの検証。
2.  **LOD 制御の実装**: Blender 側からの精度パラメータ動的変更のテスト。
