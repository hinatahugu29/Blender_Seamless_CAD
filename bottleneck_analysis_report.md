<!-- DOC-STATUS -->
> **状態: 歴史的資料** — 当時のボトルネック分析。**その後アーキテクチャが変わっている**（アウトプロセス化、インクリメンタル再開、メッシュキャッシュ、ドラッグ中の BSP プレビュー）。
> **現在の性能特性の根拠には使わないこと。**
>
> 全体の分類は `CLAUDE.md` §6 にある。**このバナーを消さないこと。**

---

# Blender CAD ボトルネック解析 & 高速化提案レポート

プロジェクト内の C++（OpenCascade）および Python ブリッジのコードを俯瞰・分析した結果、オブジェクトが増加した際、特に**ブーリアン処理**周辺でボトルネックが発生する要因と、それを劇的に改善できる具体的な高速化の余地をまとめました。

---

## 1. 最も重要なボトルネック：逐次的なブーリアン演算 (Sequential Booleans)

### 現状の問題点
[occ_core.cpp](file:///e:/blender_addon/Blender_CAD/Blender_CAD_V_8_1_2/src_rust/src/occ_core.cpp#L810-L842) のスタック評価ループでは、オブジェクト（プリミティブ）が追加されるたびに、1つずつ順番に `apply_boolean` を呼び出し、内部で `BRepAlgoAPI_Fuse` や `BRepAlgoAPI_Cut` などのブーリアン演算を個別・逐次的に実行しています。

```cpp
// occ_core.cpp L828 付近
TopoDS_Shape merged = apply_boolean(comp_base, prim, p_op, &current_face_map, true);
```

オブジェクト数が $N$ 個に増えてくると、各ステップで毎回トポロジーの再構築や履歴（History）の更新処理が行われるため、計算コストが累積的に増大します。特に同じ操作（例：連続する `ADD` や、複数のツールオブジェクトによる `SUB`）であっても、1回ずつブーリアンを繰り返すため非常に非効率です。

### 改善案：バッチブーリアン (Batch Booleans) の導入
OpenCascade の `BRepAlgoAPI_BooleanOperation` (Fuse, Cut, Common) は、引数として複数の形状リスト (`TopTools_ListOfShape`) を一括で受け取ることができます。
- **連続する同一操作をまとめる**: スタック内で連続する `ADD`（または `SUB`）のプリミティブを一時的にリストに集約し、1回のブーリアン API 呼び出し（`SetTools` にリストを渡す）で処理します。
- **効果**: 交差判定や境界表現 (B-Rep) の再構築、履歴追跡が1回で済むため、オブジェクト数が多くなるほど劇的な速度向上（数倍〜数十倍）とエラー発生率の低下（堅牢性の向上）が見込めます。

---

## 2. バウンディングボックス交差判定（クラスタリング）の高速化

### 現状の問題点
[occ_core.cpp](file:///e:/blender_addon/Blender_CAD/Blender_CAD_V_8_1_2/src_rust/src/occ_core.cpp#L815-L820) では、バウンディングボックス (`Bnd_Box`) が干渉しているクラスタのみを対象にブーリアンを行うという非常に優れたアプローチが取られています。
しかし、この交差判定は既存の全クラスタに対して**線形走査（ループによる総当たり）**で走っています。

```cpp
// occ_core.cpp L817 付近
for (size_t c = 0; c < current_clusters.size(); ++c) {
    ensure_cluster_bbox(current_clusters[c]);
    if (!prim_box.IsOut(current_clusters[c].bbox)) interacting_indices.push_back(c);
}
```

### 改善案：空間インデックス (BVH / R-Tree) の活用
オブジェクト数が数百〜数千規模に増えた場合、クラスタの数自体が増え、この単純ループ判定がボトルネックになります。
- **対策**: OCCT の `BOPTools_BoxTree` などの境界ボリューム階層 (BVH) もしくは R-Tree を用いてクラスタのバウンディングボックスを管理することで、交差するクラスタの検索を $O(N)$ から $O(\log N)$ に削減します。

---

## 3. `BOPAlgo_GlueEnum`（接着オプション）の有効活用

### 現状の問題点
[occ_booleans.cpp](file:///e:/blender_addon/Blender_CAD/Blender_CAD_V_8_1_2/src_rust/src/occ_booleans.cpp#L41) などのブーリアン処理において、接着モード (`SetGlue`) がコメントアウトされています。
```cpp
// b->SetGlue(BOPAlgo_GlueShift); // Enable for benchmark testing if coplanar
```

面同士が完全に一致（コプレーナ）している形状や、交差がまったくない境界部分が接触している形状同士のブーリアン演算では、不要な交差再計算が発生し、パフォーマンスが低下します。

### 改善案：形状関係に応じた Glue オプションの自動設定
- `BOPAlgo_GlueShift` や `BOPAlgo_GlueFull` を適切に設定してブーリアンを実行することで、交差ジオメトリの計算をスキップし、トポロジカルなマージのみを行うため、計算が劇的に高速化されます。

---

## 4. 依存関係グラフに基づく部分更新 (Incremental Update) の導入

### 現状の問題点
現在のハッシュによるキャッシュ管理は、スタック（ヒストリ）の順序に依存しています。スタックの「前の方」にあるオブジェクトを Blender 側でトランスフォームしたりパラメータを変更したりすると、それ以降のすべてのオブジェクトのハッシュ値が変わり、無関係なブーリアン処理まで全再計算が走ってしまいます。

### 改善案
- **干渉グラフの作成**: 各オブジェクトがどのオブジェクトとバウンディングボックスレベルで干渉しているかの依存関係グラフ（干渉グラフ）を保持します。
- **部分再計算**: 変更されたオブジェクトと、トポロジカルに干渉しているクラスタのみを再評価するようにし、影響のないクラスタはキャッシュ（`TopoDS_Shape`）をそのまま再利用します。これにより、スタック順序に関係なく、編集時のレスポンスが極めて高速になります。

---

## 5. インタラクティブプレビュー時のメッシュ細分化（Deflection）の動的制御

### 現状の問題点
[occ_mesh.cpp](file:///e:/blender_addon/Blender_CAD/Blender_CAD_V_8_1_2/src_rust/src/occ_mesh.cpp#L143) などでメッシュ作成を行う際、`deflection` パラメータが渡されていますが、プレビュー品質は全体で一貫した値が使われる傾向があります。

### 改善案
- **ドラッグ操作時（インタラクティブ）**: トランスフォームのドラッグ中やパラメータ変更のスライダー操作中は、`deflection` 値を意図的に粗く（例えば `0.8` 以上など）設定して超高速にメッシュ作成・描画を行います。
- **操作完了時（静止時・ベイク時）**: マウスを離したタイミングや操作確定時にのみ、高品質（`0.1` など）で最終メッシュを再生成します。
- これにより、オブジェクト数が増えた状態でも、操作中のフレームレート（FPS）を高く維持することが可能になります。
