<!-- DOC-STATUS -->
> **状態: 歴史的資料** — Sweep / Loft の実装計画。**実装済み**（`INVENTORY_V8_1_5_1.md` §2 の Sweep / Loft / FaceLoft）。
> かつて `Sweep_Loft 実装計画案.md` という**バイト単位で同一の重複**が存在したが、2026-08-17 に削除して本ファイルへ一本化した。
>
> 全体の分類は `CLAUDE.md` §6 にある。**このバナーを消さないこと。**

---

# Sweep / Loft 実装計画案

CADならではの強力な曲面生成機能である「スイープ（Sweep）」と「ロフト（Loft）」を当アドオンに統合するための実装計画です。

## Goal Description

単なる直線の押し出し（Extrude）ではなく、任意の軌道（パス）に沿った押し出しや、複数の異なる断面を滑らかに繋ぐ機能を追加し、有機的・工業的な複雑な曲面モデリングを可能にする。

---

## 1. スイープ（Sweep）機能の実装計画

**概要**: 断面（プロファイル）を、指定した経路（パス/レール）に沿って押し出す機能。

### Proposed Workflow & UI
1. **事前準備**: ユーザーが2つのスケッチを作成する。
   - スケッチA（プロファイル）: 押し出したい断面（例：小さな円）
   - スケッチB（パス）: 押し出しの軌道となる線（例：波打つスプライン曲線）
2. **コマンド実行**: UIパネルから「Sweep」ボタンを押す。
3. **対象の選択**: モーダルUIが立ち上がり、ユーザーに「断面」と「パス」を順に選択させる。
4. **データ構築**:
   - Primitive JSONとして新しいオペレーション `SWEEP` を定義。
   - プロファイルの情報と、パスの情報を参照（UUIDやワイヤーデータ）として格納してRustエンジンへ送る。

### Proposed Changes (Rust/C++ Core)
- **C++側の処理追加 (`occ_core.cpp`)**:
  - `SWEEP` オペレーションを受け取った際、2つのワイヤー（`profile_wire` と `path_wire`）を構築する。
  - `BRepOffsetAPI_MakePipe pipe_maker(path_wire, profile_wire);` を使用してスイープ形状を生成する。
  - 生成されたソリッド（またはシェル）を `out_shape` として返す。

---

## 2. ロフト（Loft）機能の実装計画

**概要**: 空間に配置された複数の異なる断面（スケッチ）を、滑らかな曲面で繋ぐ機能。

### Proposed Workflow & UI
1. **事前準備**: ユーザーが高さの異なる複数の平面に、複数のスケッチ（断面）を描いておく。
   - 例：底面に四角形、中段に円、上段に三角形。
2. **コマンド実行**: UIパネルから「Loft」ボタンを押す。
3. **対象の選択**: モーダルUIが立ち上がり、ユーザーに「繋ぎたい順序」でスケッチを順番にクリックさせる。
4. **データ構築**:
   - Primitive JSONとして新しいオペレーション `LOFT` を定義。
   - 選択された複数のスケッチの参照（UUIDリスト）を順序を保ったままRustエンジンへ送る。

### Proposed Changes (Rust/C++ Core)
- **C++側の処理追加 (`occ_core.cpp`)**:
  - `LOFT` オペレーションを受け取った際、渡された順序に従って複数のワイヤー（`wire_1`, `wire_2`, ...）を構築する。
  - `BRepOffsetAPI_ThruSections` クラスを使用する。
    - `BRepOffsetAPI_ThruSections generator(Standard_True);` （ソリッドを作るフラグをTrueにする）
    - 各ワイヤーを `generator.AddWire(wire);` で順番に追加する。
    - `generator.Build();` で滑らかなロフト形状を生成する。

---

## Verification Plan

### Automated Tests
- C++レイヤーで、円と曲線のワイヤーをプログラムで渡し、`MakePipe` によってソリッドが生成されること（体積が0より大きいこと）を確認する。
- 異なる位置にある2つのワイヤー（四角と円）を渡し、`ThruSections` でエラーなく形状が生成されることを確認する。

### Manual Verification
- Blender上で、円のスケッチとスプライン曲線のスケッチを描き、Sweepを実行して「うねったパイプ」が正しくプレビュー・生成されることを確認する。
- 高さが違う3つのスケッチ（四角、円、四角）を描き、Loftを実行して「くびれのある花瓶のような形状」が生成されることを確認する。
