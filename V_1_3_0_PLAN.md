<!-- DOC-STATUS -->
> **状態: 歴史的資料** — V1.3.0 のチェックリスト。**実施済み。**インクリメンタル・キャッシュと WGPU 描画は現在の構成に入っている。
>
> 全体の分類は `CLAUDE.md` §6 にある。**このバナーを消さないこと。**

---

# V1.3.0 実装状況チェックリスト: "Zero-Latency Engine"

> [!IMPORTANT]
> **V1.3.0 / V1.3.1 の到達目標**
> Python の重いリスト処理を排除し、Rust/WGPU とインクリメンタル・キャッシュによって「計算待ち」を感じさせないモデリング体験を実現する。

---

## 1. 計算の並列化（Rayon によるマルチスレッド駆動）
OCCTの重量級処理をマルチコアCPUに最適化。

- [x] **並列テッセレーション**
    - `generate_mesh` 内でフェイスごとの抽出を `par_iter` で実行。
- [/] **独立ブーリアン実行**
    - 現在はシーケンシャルな累積計算。ただし V1.3.1 のキャッシュにより、実質的な待機時間は解消。
- [x] **スレッドセーフな型管理**
    - `Arc`, `Mutex` による WGPU リソースとキャッシュの安全な管理。

## 2. GPU駆動描画（WGPU による「脱・Python」パイプライン）
描画の最短経路を構築し、Python のオーバーヘッドを粉砕。

- [/] **Shared GPU Buffer**
    - 完全なハンドル共有ではないが、`memoryview` による高速転送で実用上のボトルネックを解消。
- [x] **Zero-Copy 連携**
    - `[x, y, z]` リスト変換を完全廃止。バイナリデータを直接テクスチャとして流し込みに成功。
- [x] **WGPU 外部テクスチャ描画**
    - ビューポートへの WGPU オーバーレイ合成を安定化。描画速度が劇的に向上。

## 3. インクリメンタル・キャッシュ（汚染フラグ管理）
「変更された部分だけ計算する」というサボりの極致。

- [x] **Granular Dirty Flags**
    - モディファイア単位のハッシュチェックによる「汚染インデックス」の特定機能を実装。
- [x] **ジオメトリ・メモイゼーション**
    - C++ / Rust 両レイヤーでの中間結果キャッシュを実装。移動や追加時の再計算がほぼゼロに。

## 4. インタラクティブ・LOD（動的精度制御）
操作感のさらなる追求。

- [ ] **Drag-time Deflection**
    - マウスドラッグ中のみ精度を落とし FPS を優先する動的制御（今後の課題）。

---

## 📅 実装スケジュール・ステータス

- [x] **Week 1: Rayon 統合と並列化の検証**
    - 並列メッシュ抽出による高速化を確認。
- [x] **Week 2: WGPU 基盤の構築**
    - ビューポート描画を WGPU Overlay に切り替え完了。
- [x] **Week 3: パイプライン結合と最適化**
    - インクリメンタル・キャッシュ (V1.3.1) の統合を完了。

---

> [!TIP]
> **現在の体験レベル**
> 「数十個のオブジェクトがあっても、最後尾のパーツを動かす分には計算遅延がほぼ発生しない」状態に到達しました。
