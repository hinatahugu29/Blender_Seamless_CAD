# Blender CAD 出品準備メモ

現状の判断:
- Windows限定なら、出品準備を始めてよい段階。
- 実機2台で `ZIPインストール -> 起動 -> 動作` を確認できているのは強い。
- ただし「広く売る」前に、販売ページで明示すべき制約と、最低限そろえるべき情報は残っている。

## 今の強み

- 配布ZIPをそのままインストールして動作確認済み
- 自宅PCと職場ノートPCの2系統で確認済み
- Windows限定という前提を明確に置ける
- `CAD_8_1_0_install.zip` のように、配布物がかなり完成形に近い

## 出品前の最低ライン

- 対応OSを `Windowsのみ` と明記
- 対応Blenderバージョンを明記
- `Beta` 扱いであることを説明欄に明記
- インストール手順を 3-5 行で書く
- 最低1本、導入から基本操作までの短い動画かGIFを用意
- 主要機能を 5-8 個に絞って列挙
- 非対応事項を明記
- 問題発生時の連絡先または更新方針を明記

## 先に確認したい項目

- `Blender_CAD_V_8_1_0/CAD_8_1_0/__init__.py` は `warning = "Beta"` になっている
- 配布物には多くの DLL / 同梱ライブラリが含まれるため、ライセンス表記の整理が必要
- 古い `addon/__init__.py` には開発機の固定パスが残っているが、現行の出品候補は `CAD_8_1_0` 系を前提にする

## 販売ページに書くべき制約

- Windows専用
- Blender 4.2 以上想定
- ベータ版
- すべてのPC環境で完全同一動作は保証しない
- 内蔵GPU環境でも動作実績はあるが、重いシーンでは性能差が出る可能性がある
- 将来的に仕様変更の可能性がある

## 商品説明のたたき台

`Project Seamless CAD` は、Blender上でネイティブにCADワークフローを扱うためのアドオンです。
プリミティブ作成、ブーリアン系操作、スケッチ系操作、STEP/SVG関連機能を1つの作業系にまとめています。

本バージョンは Windows 環境向けのベータ提供です。
ZIPからインストールして使用でき、自宅PCと内蔵GPUノートPCで基本動作を確認しています。
一方で、すべての環境で完全に同じ動作を保証する段階ではないため、ベータ版としての提供になります。

## スクリーンショット候補

- メインUI全体
- スケッチ作成中
- ブーリアン編集中
- STEPまたはSVGの入出力例
- 最終ベイク後の見た目

## 最低限の検証チェックリスト

- ZIPから新規インストールできる
- アドオン有効化でエラーが出ない
- 基本パネルが表示される
- 新規CAD開始ができる
- Box追加ができる
- 1つ以上の編集操作が通る
- ベイクできる
- 保存 -> 再読込後に致命的不整合が起きない
- Undo/Redoで即破綻しない
- STEPまたはSVGの主要導線が最低1本通る

## 出品直前に詰めたいもの

- `対応Blenderバージョン`
- `既知の不具合`
- `非対応機能`
- `更新予定`
- `問い合わせ先`
- `ライセンス表記`

## 判断メモ

「完成してから出す」より、「Windows限定ベータとして制約を明示して出す」の方が現実的。
今の段階で重要なのは、完成度を過剰に見せることではなく、できること・まだ荒いこと・対象環境を正直に書くこと。
