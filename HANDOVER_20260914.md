# 引継書 — 8.1.5.16（2026-09-14）

> **状態：現行。** 配布前の引継ぎ。配布が済んだら
> 「歴史的資料」へ移し、このバナーを書き換えること。

## 1. 一行で

ロードマップ 3.1（変数と数式）と 3.2（Suppress・リネーム）を 8.1.5.15 で実装し、
**実機確認まで終えた（異常なし）**。そのあと配布を急がず、**8.1.5.16 で足場を3つ
固めた**：不具合の記録、rustc の固定、カーネルの版ハンドシェイク。

**8.1.5.15 の ZIP は配らない。** カーネルが変わったので 8.1.5.16 で作り直す。
Windows ZIP は作成済み、macOS / Linux は **CI 未実行**。

### 8.1.5.16 で足場を固めた4点（2026-09-14）

1. **Face Inset の不具合を記録した。** 9月の報告は「Linux/macOS で落ちなくなった」
   で閉じたが、**原因は取り除いていない**。Windows では落ちない代わりに形が壊れる。
   録画 `broken/broken.webm` と報告者への返信3本が唯一の記録で、どちらもリポジトリの
   外にあった。`docs/*/limitations.md` と `FEATURE_GAPS.md` 6. に書いた
2. **rustc を 1.98.1 に固定した**（`src_rust/rust-toolchain.toml`）。固定前は
   カーネルのソースを変えていないのに CI のビルドごとにバイナリのサイズが動いていた。
   §6 に「原因未調査」と書いてあった 1,456 バイトの正体がこれ
3. **カーネルが版を名乗るようにした**（`kernel_info`）。`is_server_alive` の
   docstring に「本来はハンドシェイクにすべき」と書いてあったもの。報告者にも
   約束していた
4. **OCCT が壊れた形を返したフィレット/面取りを捨てるようにした**
   （`blend_result_within_bounds`）。利用者のモデル `20260914-1.blend` で、
   稜線が穴の円筒に接して終わる位置のフィレットが、**どの半径でも**外へ伸びる面を
   作っていた。体積も面積ももっともらしい値になるので bbox でしか検出できない。
   **未解決：合成モデルでの再現テストは書けていない**（ガードを外しても通って
   しまったので入れなかった）。裏付けはその .blend での実測

## 2. 今どこにあるか

| もの | 場所 | 状態 |
|---|---|---|
| ソース | `main` | **push 未実施**（8.1.5.16 の5コミット） |
| Windows ZIP | `Blender_CAD_V_8_1_5_1/CAD_8.1.5.16_install.zip` | PREFLIGHT OK（40.9 MB）。ガード入りで作り直し済み |
| Linux ZIP | `MAC_LINUX/CAD_8.1.5.16_install_LINUX.zip` | CI run 34843289274。**WSL で回帰テスト 59/0/0 実施済み**（Blender 5.2.1） |
| macOS ZIP | `MAC_LINUX/CAD_8.1.5.16_install_MAC.zip` | 同 run。実機未確認 |

> **3プラットフォームとも作り直してある。** フィレット/面取りのガードを入れた時点で
> カーネルが変わり、**同じ 8.1.5.16 で中身の違うカーネルが2つある状態**になったため。
> 未配布なので番号は据え置き。旧 run 34837616508 の ZIP は破棄済み。
| 旧 8.1.5.15 の ZIP | ルートと `MAC_LINUX/` | **配らない**（カーネルが古い） |
| 商品説明 | `SUPERHIVE_PRODUCT_DESCRIPTION_20260811.html` | 8.1.5.15 の更新欄を追加済み。**8.1.5.16 の分は未記載・未貼り付け** |
| マニュアル | `docs/en` `docs/ja` の `feature-tree.md` | 追記済み。ru/zh は該当ページ無し |

8.1.5.13 / 8.1.5.14 は**配らない**。13 はバインドが複製で消える、14 はドラッグ中の
変数変更が失われる。14 の macOS/Linux は `MAC_LINUX/past/`、Windows 版 ZIP は削除済み。

## 3. 配布前に残っていること

1. ~~**実機確認（8.1.5.15、Windows）**~~ — **2026-09-14 に実施、異常なし**
2. ~~**macOS / Linux の ZIP を CI で作り直す**~~ — **完了**（run 34837616508、
   `MAC_LINUX/` に配置済み、README 更新済み）
3. **実機確認（8.1.5.16、Windows）** — 下のチェックリスト。**カーネルを変えたので
   C・D をもう一度通すこと**
4. **Superhive へ貼る** — 商品説明に 8.1.5.16 の更新欄を足してから
5. ~~（任意）Linux ZIP で回帰テスト~~ — **完了。59/0/0**
   （`Blender_CAD_V_8_1_5_1/tools/wsl/verify_linux_build.sh`、Blender 5.2.1）
6. 配布したらこの引継書のバナーを「歴史的資料」に変える

### 実機チェックリスト

8.1.5.14 で確認済み（再確認は任意）：

- [x] Feature Tree の目のアイコンで、途中の行を抑制・復帰できる
- [x] Active Property Editor で改名し、ビューポートを触っても戻らない
- [x] Parameters パネルで変数を作り、Drive a Field で Size X を式に結び、変数変更で形が追従する
- [x] ギズモでプロキシを動かし、ドラッグ追従・確定後の描画が従来通り（チェックリスト C・D）

**8.1.5.16 で未確認（カーネルが変わったので、ここだけは必ず）：**

- [ ] 新しい Blender で起動し、コンソールに版の不一致の警告が**出ない**こと。
      出るなら、古い `cad_server` が 8080 に残っているか、`deploy.py` を通していない
- [ ] ドラッグ追従と確定後の描画（チェックリスト C・D）

逆の確認（任意、一度やっておくと安心）：古い 8.1.5.15 の `cad_server` を先に
起動しておいてから Blender を開くと、`kernel build mismatch` がコンソールに出る。

**8.1.5.15（2026-09-14 に実機で確認、いずれも異常なし）：**

- [x] 式で決めた欄を持つプロキシを**ドラッグで動かし、離した直後に**変数の値を変える。
      ドラッグ終了後に形が**変数の値**になること（旧版では古い値のまま残った）
- [x] 上の操作のあと、ドラッグ追従と確定後の描画が従来通り（C・D）

失敗したら：`core/parameters.py` の `_retry_after_drag` / `resume_after_drag`
（ドラッグ終了を 0.2 秒間隔のタイマーで待って `apply_parameters` を呼び直す）を疑う。

## 4. 何を変えたか

| 版 | 内容 | 主な場所 |
|---|---|---|
| 8.1.5.13 | Suppress、リネーム、変数と数式 | `properties.py` `core_bridge.py` `core/parameters.py` `operators/parameters.py` `ui/ui_main_panel.py` |
| 8.1.5.14 | 複製・グループ化でバインドを保持、変数改名で式を追従 | `operators/management.py` `properties.py` |
| 8.1.5.15 | ドラッグ中の変数変更を、ドラッグ後に再適用 | `core/parameters.py` |

**カーネル（Rust/C++）は一切変えていない。** Python のみ。

### 設計の要点（コードにもコメントあり）

- **Suppress**：`core_bridge.py` の履歴組み立てで行を飛ばすだけ。抑制した行はターゲットを消費しない
- **式の評価は `eval()` を使わない**。AST ホワイトリスト。`.blend` 経由の任意コード実行を防ぐため
- **配置の欄（size/location/rotation）はプロキシの行列が正**。書いただけでは depsgraph ハンドラに
  巻き戻されるので、書いた後に `sync_proxies` で押し出す。ドラッグ中は押し出せないので後で再適用
- **リネーム**：プロキシ同期がオブジェクト名で `prim.name` を上書きするので、名前の update でプロキシも改名

## 5. 検証の記録

- 回帰テスト：**59 passed / 0 failed / 0 skipped**（Blender 5.2.1、他のサーバー無し）。
  8.1.5.16 で1本増えた（`kernel reports its build`）
- `kernel_info` の実動確認（2026-09-14）：新カーネルは
  `{kernel_build: 8.1.5.16, protocol: 1, exe, size, pid}` を返す。未知のアクションは
  空応答。8.1.5.15 のカーネルに投げると空応答＝「古い」と判定できることも実測
- 破壊検証：`_EXPECTED_KERNEL_BUILD` を偽の値にすると、**その1本だけ**落ちる
  （58 passed / 1 failed）
- 8.1.5.16 の Windows ZIP：`package_addon.py` で PREFLIGHT OK、40.9 MB
- **Linux の配布 ZIP で回帰テスト：59 passed / 0 failed / 0 skipped**（WSL2 /
  Blender 5.2.1）。CI の Face Inset 再生も両プラットフォームで通っている。
  この過程で Blender 5.1.1 の undo クラッシュの条件が判明した（§6）
- CI の rustc 固定が効いていることをログで確認：両プラットフォームとも
  `rustc 1.98.1 (48a229cea 2026-09-01)`
- フィレットのガード導入後も、作り直した Linux ZIP で **59 passed / 0 failed**
  （Blender 5.2.1）。利用者のモデルでは全ての半径が「そのフィレットを抑制した
  結果」と完全一致（体積 0.699394、bbox ±0.5）
- 実機確認（Windows、`Install...` で 8.1.5.15）：**2026-09-14、異常なし**。
  ドラッグ直後の変数変更・ドラッグ追従・確定後の描画（C・D）
- 新規テスト5本（suppress / rename / 評価器の安全性 / 変数で形が追従 / 複製と改名 / ドラッグ中）
- 破壊検証：Suppress のスキップを外す → 落ちる。ドラッグ後の再適用を外す → 落ちる
- `audit_ui_params.py`：exit 0（`suppressed` を IGNORE に追加。1行だけで試す監査の構造上の誤判定）
- macOS/Linux ZIP：`bl_info`、.py のソースとのバイト一致、`libs`、修正の有無を確認。詳細は `MAC_LINUX/README.md`

## 6. 調べて分かったこと（直していないもの）

| 事象 | 結論 |
|---|---|
| **Blender 5.1.1 の Linux で undo が Blender ごと落ちる** | **2026-09-14 に条件を特定。** `std::bad_optional_access` で abort。**5.1.1 では必ず落ち（6/6）、5.2.1 では完走（59/0/0）。アドオンの版は無関係** — 8.1.5.15 を当時のテストで回しても 5.1.1 では落ちる。`CROSS_PLATFORM_BUILD.md` §8-5 に実測。**未分離：Blender 側の不具合か、こちらの触り方が 5.1.1 でだけ露呈するのか。実機 Linux も未検証** |
| ARC で `CAD Server Error: CAD Update Failed` | 監査が UI に出ていない `extrude_height` を pipe モードのまま動かしたときだけ。**利用者は踏めない**。8.1.5.12 でも同じ |
| 抑制した行の下のフィレット | 一部の辺だけ残る／エラー無しで消える。抑制を戻せば復帰。辺参照の性質なので **docs に明記して仕様とした** |
| 式で決めた欄を手で動かす | 次に変数が変わると式の値へ戻る。**パネルに注意書きを出して仕様とした** |
| Linux カーネルが 8.1.5.12 より 1,456 バイト大きい | **解明済み（2026-09-14）**。CI が rustc を固定しておらず、ランナー同梱の版が上がっていた。両バイナリの差は rustc のソースパスハッシュだけ（`88d9e12a`→`48a229ce`）で、C++ 側は同一。`rust-toolchain.toml` で 1.98.1 に固定した |

## 7. 次にやるなら

**まず `FEATURE_GAPS.md` 6.（Face Inset の破綻）を見ること。** 機能の穴ではなく、
利用者から報告の来ている不具合で、まだ原因が残っている。

そのうえで新機能なら `IMPLEMENTATION_ROADMAP.md` の順で
**2.1 スケッチのプロジェクト（既存エッジの投影）**。
まず「一度きりの取り込み」だけ。着手前に `core/semantic_targets.py` を読む。

## 8. このセッションで踏んだ罠（次の人向け）

- **Blender は `D:\SteamLibrary\...`**（CLAUDE.md は修正済み）
- **ポート 8080 に他人のサーバーがいると回帰テストが一部スキップになる。**
  Roaming にインストールした GUI の Blender が動いていると起きる。そのサーバーは落とさない
  （利用者のセッションが壊れる）。Blender を閉じてから回す
- **調査用に `addon.register()` して `os._exit` するスクリプトはサーバーを残す**可能性がある。
  パスを見て、リポジトリのカーネルなら落としてよい
