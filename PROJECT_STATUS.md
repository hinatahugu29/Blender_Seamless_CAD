# Blender CAD Project (Seamless CAD) - ステータス報告

## 1. プロジェクト概要
Blender内で エンジンを直接駆動し、ノンデストラクティブなCADモデリング体験を提供するアドオン。Blenderのメッシュベースの操作感と、CADの正確な幾何計算を融合させることを目的としています。

## 2. 現在の進捗・実装済み機能
- **コアエンジン**: Rust + OpenCascadeによる高速な幾何計算ブリッジの実装。
- **ノンデストラクティブ・ワークフロー**:
  - プリミティブ（Box, Cylinder, Sphere, Curve, Arc等）のリアルタイム編集。
  - Add (Fuse), Subtract (Cut), Intersect (Common) による動的なブーリアン演算。
  - フィレット（Fillet）および面取り（Chamfer）のモディファイア的適用。
- **高精度エッジ・ピッキング**:
  - `Extrema`（幾何学的最短距離計算）を用いた高精度な辺選択。長い直線の中央でも確実にヒットします。
- **ビューポート統合**:
- **安定性向上 (2026-05-14 アップデート)**:
  - **ノートPC/内蔵GPU対応**: メモリ管理の最適化とデータ転送の軽量化により、Intel Iris Xe等の内蔵GPU環境でのクラッシュを完全に克服。
  - **計算の堅牢化**: `FuzzyValue(1e-4)` の導入と `ShapeUpgrade_UnifySameDomain` によるトポロジー自動クリーンアップの実装。複製作成時や面の一致するブーリアン演算の成功率が大幅に向上しました。
  - **スケッチの安定化**: 同一点チェックと2点時の直線フォールバックにより、スケッチ開始時のクラッシュを完全に排除。
  - **操作レスポンスの極致化 (V1.3.5 追加)**:
    - **I/Oブロッキングの完全排除**: Python/Rust/C++の全レイヤーでデバッグログを沈黙させ、コンソール書き込みによるメインスレッドの微小な引っ掛かりを完全に解消。
    - **Blender標準操作の自動検知**: 独自のギズモ操作だけでなく、Blender標準の移動・回転・拡大縮小（G/R/S）をリアルタイムに検知し、自動的に高速プレビューモードへ移行する仕組みを実装。
    - **インテリジェント・スロットリング**: ドラッグ中は面メッシュの再生成（テセレーション）を完全にスキップし、ワイヤーフレームの更新のみに集中。これにより、複雑なブーリアン形状でも吸い付くような追従性を実現しました。
- **高度なエッジ・ピッキング**:
  - `Extrema`（幾何学的最短距離計算）を用いた高精度な辺選択。
  - **スレッド安全性**: Mutexによる排他制御により、形状更新中のピッキング操作によるクラッシュを解消(V1.3.2)。
- **ビューポート統合**:
  - **WGPU Zero-Latency レンダリング**: Blenderの描画ループに依存しない、Rust/WGPUによる高速なオーバーレイ描画の統合(V1.3.1)。
  - **適応型テセレーション**: ViewportとBakeで個別に精度（Deflection）を調整可能(V1.3.2)。
  - 選択モーダル実行中の視点操作（回転・パン・ズーム）の完全サポート。
- **UX/UI の改善**:
  - **直交モード (Ortho Mode)**: スケッチ中にShiftキーでX/Y軸に固定した精密な描画をサポート。
  - **リスト同期**: UIリストでの選択と3Dビューポート上のプロキシオブジェクト（Seamless_Proxy）の選択状態を完全同期。

## 3. 技術スタック
- **Frontend**: Python (Blender 4.2+ / 5.1+ 対応)
- **Backend**: Rust (Out-of-Process TCP Server, `wgpu`, `ezpz` crate)
- **Geometry Kernel**: OpenCascade (OCCT) 7.9.3

## Blender_CAD_V_7_0_1 (2026-06-03) - Sketch Solver Stabilization & Precision Fixing
- **OnFaceスケッチのローカル座標回転バグ（90度ズレ）の完全解決**:
  - 曲面や任意の面でのスケッチ時に描画が90度回転してしまう問題を修正。OpenCASCADE（C++コア）側でのオイラー角の適用（`gp_Extrinsic_XYZ`）をBlenderのXYZ回転と完全に整合させ、Python側の `up` ベクトル計算も修正することで、意図した通りの向きでのスケッチ確定を保証。
- **GCSソルバーとTCP通信の堅牢化**:
  - Rustサーバー側に不足していた `solve_sketch` ハンドラを追加し、スケッチ中の頂点ドラッグ時にGCSソルバーが通信切断される問題を解消。

## Blender_CAD_V_7_0_0 (2026-06-03) - Out-of-Process Server Architecture & Parallel Computing
- **サーバー・クライアント型（アウトプロセス）アーキテクチャへの完全刷新**:
  - 従来のインプロセスDLL（`seamless_core.dll`）から、Rust/C++による独立したCADサーバー（`cad_server.exe`）と、Blender（Pythonクライアント）間のTCP/IPC通信モデルへとアーキテクチャを根本的に移行。
  - BlenderのメインプロセスとCADカーネルのメモリ空間を完全に分離。重い演算やカーネル側のパニックが発生してもBlender自体は絶対にクラッシュしない、究極の安定性（Zero-Crash）を実現。
- **スタック単位の完全並列計算（Non-blocking UI）**:
  - Rust側のグローバルロックを解除し、CADスタック（オブジェクト）ごとの個別ロックへ移行。複数オブジェクトの重いブーリアンやフィレット計算をバックグラウンドで並列実行可能にし、計算中もBlenderのUIが一切フリーズしない極めて滑らかな操作感を実現。
- **TCPピッキングの統合**:
  - 高精度なエッジ・面選択（Extrema）処理もTCP通信経由へ移行し、遅延を感じさせないリアルタイムなハイライトを実現。

## Blender_CAD_V_3_0_0 ~ V_6_0_X (2026-05-29 ~ 2026-06-02) - Core Performance Tuning & Architecture Iterations
- **パフォーマンス最適化と検証の反復**:
  - シェーダーのBlender 4.x/5.x対応や、フィレット/ブーリアン処理の最適化（UnifySameDomain）、メッシュ生成順序の改善（V5）など、インプロセスでの限界を探る様々な検証を実施。
  - 複数オブジェクト時の著しいパフォーマンス低下やUIの「もっさり感」を解消するため、C++コアとRustブリッジの大規模な分析とリファクタリングを敢行。これが最終的なV7（サーバー・クライアント型）への布石となる。

## Blender_CAD_V_2_0_4 (2026-05-21) - Pure CAD Arc & Circle Native Reconstruction (OpenCASCADE BRepBuilderAPI_MakeWire & MakeFace Integration)
- **円弧カクツキ問題の根本的解決（ネイティブ幾何サーフェスの再構築）**:
  - **Rust FFI配列フラット化伝送**: Python側から渡された `segments` 情報をC++（OpenCASCADE）側に最適化して渡すため、1セグメントあたり17個の `double` 値（`type`, `start(3)`, `end(3)`, `mid(3)`, `center(3)`, `radius`, `normal(3)`）からなるフラットな伝送配列 `all_segments` および `segment_counts` を新規設計し、RustからC++へ高速伝送するデータ構造を確立。
  - **OpenCASCADE BRepBuilderAPI によるネイティブ線・円弧・円生成**: [occ_core.cpp](file:///g:/blender_addon/Blender_CAD/Blender_CAD_V_2_0_4/src_rust/src/occ_core.cpp) において `make_curve_or_surface_from_segments` を実装。
    - `LINE` は `BRepBuilderAPI_MakeEdge`
    - `ARC` は `GC_MakeArcOfCircle` を介したネイティブ円弧
    - `CIRCLE` は `gp_Circ` と法線を用いたネイティブ真円
    としてそれぞれ正確なエッジを構築。`DELIMITER` (マジックナンバー 3.0) をトリガーとして外枠と内穴のワイヤー（`TopoDS_Wire`）を個別に束ね、`BRepBuilderAPI_MakeFace` と `BRep_Builder().Add` を用いて、OpenCASCADE内部で「真の幾何曲線からなる穴空き面」として直接合成。
  - **ShapeFixによるトポロジー自動クリーンアップ**: 生成された面に対し `ShapeFix_Face` を適用して微小な隙間や面の向き（Orientation）を自動クリーンアップすることで、後続の 3D 幾何モディファイア（`FILLET` や `CHAMFER` など）をかけた際の演算成功率とトポロジー安定性を格段に向上。
- **アルファリリースに向けたクリーン・パッケージング**:
  - **bl_info 警告フラグの更新**: 配布時アドオン情報として `"warning": "Alpha"` を設定し、アルファ版であることを明示。
  - **パッケージング自動化スクリプトの追加**: 不要な `__pycache__` やキャッシュ・一時ファイルを自動的にフィルタリングし、常にクリーンな配布用ZIPアーカイブを生成する `package_addon.py` を追加。

## Blender_CAD_V_2_0_2 (2026-05-20) - 2D Sketcher Multi-Profile (Outer & Inner Hole) Auto-Extrusion & Boundary Delimiter
- **スケッチモードでの複数プロファイル（島と穴）の自動包含判定と穴あき面（SURFACE）生成機能の実装**:
  - **閉ループの自動分類（シューレース面積＆Winding Number法）**: スケッチされた円や閉じたポリラインを検出し、シューレース公式（Shoelace formula）で面積を算出した上で、面積の降順にソート。代表点を用いた Point-in-Polygon 判定を行うことで、外枠（島）と内穴（穴）の関係を自動で幾何構造的に分類するロジックを [modal_sketch.py](file:///e:/blender_addon/Blender_CAD/Blender_CAD_V_2_0_2/CAD_2_0_2/modal_sketch.py) に実装。
  - **デリミタ `1e10` による境界結合**: 各島（外枠）に対して1つの SURFACE プリミティブを作成し、外枠頂点群の後に `(1e10, 1e10, 1e10)` という特殊なデリミタ頂点を挟んで内穴の頂点群を結合して格納する、堅牢かつ柔軟なマルチプロファイルシリアライズ形式を導入。
  - **Rust/C++コアによる複数ループの面構築**: [occ_core.cpp](file:///e:/blender_addon/Blender_CAD/Blender_CAD_V_2_0_2/src_rust/src/occ_core.cpp) の幾何生成関数を拡張。デリミタ `1e10` を検知して頂点列を分割し、外枠から作成した `TopoDS_Face` に対して `BRepBuilderAPI_MakeFace::Add` で内穴のワイヤ群を直接追加することで、OpenCASCADE レベルで穴の開いた単一の面（Face）を直接構築することに成功。これにより、スケッチを確定するだけで、追加のBoolean操作不要で即座に穴あき立体（Extrude）が作れるシームレスなUXを実現。
- **実機統合テストの整備**:
  - 新規テスト [test_multi_profile_t.py](file:///e:/blender_addon/Blender_CAD/Blender_CAD_V_2_0_2/test_multi_profile_t.py) を追加し、Blenderのバックグラウンドモードで外枠・穴・独立した島の判定およびFace生成の全体フローがエラーなく動作することを確認完了。

## Blender_CAD_V_2_0_1 (2026-05-20) - 2D Sketcher Topology Loop Fillet, Rotation Tracking, Robust Constraint Sandbox & Precision Snapping
- **スケッチフィレット (Corner Fillet) のトポロジー接続およびソルバー解決時の自動同期・反転防止の完了**:
  - **16分割直線セグメント接続**: フィレット適用時に円弧オブジェクトを追加するだけでなく、始点と終点の間に16本の直線セグメント（中間頂点群）を自動生成して接続。スケッチ全体の閉じたループトポロジーに完全に組み込まれ、面張り（Surface）や押し出し（Extrude）が即座にエラーなく機能する状態を実現。
  - **ソルバー解決時の劣弧反転・巨大化防止**: GCSソルバー解決直後に、円弧の中心・始点・終点から数学的に一意に求まる正しい劣弧の中間点へと `pt_mid` 座標を自動強制補正する「劣弧反転防止ロジック」を実装。GCSソルバーの多価性による反転・崩壊バグを完全に根絶。
- **フィレットの角度追従（回転）と後からの半径調整UIのサポート**:
  - **直交 (PERPENDICULAR) による滑らかな接合追従**: スケッチ内の直線が回転・変形した際にフィレットが崩れるのを防ぐため、従来のTANGENT拘束の代わりに、端点での直交関係を示す2つの `PERPENDICULAR` 拘束（直線と半径線の直交）を追加。これにより、直線がどのように回転・移動しても、フィレット円弧が滑らかな接続状態を保ったまま一意の姿勢へ完璧に自動回転追従する構造を確立。
  - **インテリジェント半径検出UI**: 円弧の辺やセグメント頂点をクリックするだけで、サイドバーUIの「選択情報」に『円弧半径 (Arc Radius)』が自動出現するフォールバック検出ロジックを実装。数値を入力変更するだけで、フィレットの度合いを後からパラメトリックかつリアルタイムに変形調整することが可能に。
- **円弧・円構築時の既存頂点スナップズレ防止（スナップ端点座標の強制適用）**:
  - **端点座標の精密吸着**: ARC（円弧）および CIRCLE（円）ツールにおいて、クリック時に既存頂点にスナップしている場合、生のマウス座標の代わりにスナップ先頂点の正確な既存座標を使用するように修正。スナップ操作時に直線側が歪んで折れ曲がる不具合を根絶。
  - **セグメント同期DFSの安全ガード**: `solve_gcs_external` 内の円弧セグメント頂点座標の同期DFS処理において、通過する中間ノードが `is_segment == True`（セグメント頂点）であることを強制するガードを追加。無関係な直線パスへの誤上書きや同期漏れを100%防止。
- **ドラッグ変形時のフィレット円弧崩れ防止と幾何破綻時の自動ロールバック（堅牢なスケッチサンドボックス）**:
  - **共有端点のドラッグ保護**: 円弧中心点ドラッグ時に、他幾何要素と共有されている端点は移動させないように除外処理を追加。
  - **劣弧中間点初期値の数学的補正**: GCSソルバー実行直前に、変形歪みによる初期値不整合を解消するため、すべての円弧の中間点を数学的に正しい劣弧の中心（始点・終点・中心からの算出位置）へ事前配置し、不収束を防止。
  - **異常変位および直線崩れの自動バリデーション**: GCS解決座標の適用前に、ドラッグ変位に対する他頂点の『爆発的な異常変位（3m以上または変位の5倍以上）』および『直線の極端な縮小（5mm以下に潰れるオーバーラップ）』を検知する幾何整合性バリデーションを導入。
  - **100%確実な座標ロールバック**: 幾何整合性の破綻検知時に、ドラッグ直前の健全な座標へ全自動でロールバックする構造を構築。これにより、極端なドラッグを行ってもアドオンがフリーズしたり形状が吹き飛んだりせず、安全に限界位置に留まる商用CADレベルの極めて堅牢な操作UXを実現。

## Blender_CAD_V_2_0_1 (2026-05-19) - 2D CAD Sketcher MVP & Dynamic N-Panel Sidebar
- **スケッチモード専用の動的Nパネル切り替えとコマンドパレット設計の確立**:
  - `is_sketch_active` フラグに応じた条件付きUIポリング（`poll`）を実装。スケッチモード中のみ3D操作用パネルを非表示にし、スケッチツールに特化した専用Nパネル（`SEAMLESS_PT_SketchPanel`）を自動出現させる切り替え構造を実現。
  - **コマンドパレット化**: Nパネルからペンツール（選択、点、辺、円弧）を切り替えられるほか、各拘束条件（固定、水平、垂直、平行、接線）の追加、ローカルアンドゥ（Undo）、確定（Apply）や破棄（Cancel）のワンクリック実行が可能に。
- **安全な製図サンドボックス（モーダル離脱の厳格化）**:
  - `ESC` や右クリックによる不意のモーダル離脱を防止。アクティブな作図ツールや選択状態をリセットする機能として再定義し、スケッチから完全に離脱するにはNパネル上の「確定 (Apply)」または「破棄 (Cancel)」の明示的クリックを必須とすることで誤操作による作業消失を根絶。
- **リアルタイム双方向同期の座標インスペクタ**:
  - 選択されたスケッチ頂点のIDとXY座標をNパネル上に動的表示。
  - 数値を手入力して変更した際、瞬時にGCSソルバーを駆動して形状変形を再解決する双方向インスペクタフローを実装。
- **2Dボックス選択（ドラッグ矩形選択）のサポート**:
  - `SELECT` ペンモード時、空の領域をドラッグすることで2D点線枠を描画。リリース時に対象領域に含まれるスケッチ頂点を一括検出して選択状態にするCAD仕様の選択体験を実現。
- **ビューポート軸ナビゲーションHUD（軸コンパス）**:
  - カメラの視点方向（3Dビュー行列）からスケッチ平面のローカルX軸（赤）とY軸（緑）を2Dスクリーン座標にリアルタイム射影し、画面の左下に方向指示インジケータ（コンパス）を描画。
  - 視点がくるくる回転しても、現在の描画軸方向が常に直感的に把握可能。ShiftキーによるOrtho固定状態を明示する軸延長ガイド線とHUDテキスト表示を追加。

## Blender_CAD_V_2_0_0 (2026-05-19) - Pure Rust GCS (EZPZ) Constraint-Driven Sketching & 3D Parametric CAD Integration
- **Pure Rust 2D GCS (EZPZ) による幾何拘束スケッチ機能の統合**:
  - スケッチ平面における点と線のリアルタイムな拘束解決機能（GCS）を統合。
  - 固定（Fixed）、水平（Horizontal）、垂直（Vertical）、距離寸法（Distance）の各拘束タイプのシリアライズと、ドラッグ中のノンブロッキング高速座標解決を実現。
- **2D スケッチから 3D 面・押し出し形状（SURFACE / Extrude）への全自動実体化パイプラインの確立（フェーズ5の完了）**:
  - スケッチ確定（Enterキー）時に、描画された頂点・エッジから一筆書きパスと接続グラフをDFS（深さ優先探索）で自動解析し、閉ループを `SURFACE`、開ループを `CURVE` として自動判別・登録する仕組みを実装。
  - C++ネイティブコアの `make_curve_or_surface` 関数において、従来の BSpline フィッティング一辺倒から `BRepBuilderAPI_MakePolygon` によるポリゴン生成へとリライト。直線の多角形スケッチでも100%確実に面（FACE）を生成できるように堅牢化。
  - Python-Rust ブリッジのシリアライズ処理（`core_bridge.py`）において `SURFACE` プリミティブの頂点データが欠落していたバグを修正し、データの伝搬を完全に貫通。
  - これにより、2D スケッチを確定するとビューポート上に即座に 3D 面が張られ、押し出し（Extrude Height）によって任意のソリッド形状を生成するシームレスなパラメトリック設計が可能に。


## Blender_CAD_V_1_4_6 (2026-05-18) - Asynchronous Thread-Safety, Real-Time Assembly Cascading & Garbage Collection
- **非同期スレッドとWGPU描画コンテキストの完全分離による『オブジェクト追加時のクラッシュ』の完全根絶**:
  - 非同期幾何計算スレッド（Rustバックグラウンドスレッド）からグラフィックスAPI（WGPU）のバッファ更新（`renderer::update_scene_edges`）へ直接触ることを100%遮断。メインスレッド（GUIスレッド）からのみ呼び出せる描画同期関数 `update_renderer_edges` を新設し、Python側のタイマーイベント（メインスレッド）から同期呼び出しするスレッド境界保護設計へ刷新。これにより、オブジェクト追加時に発生していた Windows のグラフィックスコンテキスト競合（`ACCESS_VIOLATION`）を完全に排除。
- **非アクティブコレクション編集（S/G/Rキー等）のリアルタイム玉突き連動（Cascade Update）の実現**:
  - ビューポート更新監視ハンドラー `depsgraph_update_handler` をシーン内の全 CAD パーツコレクションを動的に走査する設計へ拡張。
  - 非アクティブなパーツ（マスター）を 3D ビューポート上で直接G/R/Sキー操作した際にも変更が即座に検知され、それを参照しているアセンブリインスタンス（アクティブ）側へ玉突きのようにリアルタイム再計算が伝搬・追従する「Cascade Update」を実現。
- **Rキー（回転）によるマスター編集時のアセンブリ（インスタンス）回転同期バグの修正**:
  - 別コレクションからのインポート（アセンブリインスタンス）の際、クローン元のトランスフォーム二重適用防止のための「マスター座標逆変換相殺」処理において、位置（`m_loc`）のみを原点にキャンセルし、回転（`q_master`）のキャンセルを除外（単位クォータニオンとして回転を保持）するよう C++ コア `occ_core.cpp` の形状構築ロジックを修正。
  - これにより、マスタープロキシをRキーで回転した際、アセンブリ側のインスタンスも完璧にリアルタイム追従して傾きが同期・生成されるようになり、自由なアセンブリレイアウト設計が可能に。
- **コレクション削除時のGPU描画バッファ＆C++ネイティブメモリの全自動GC（ガベージコレクション）の実装**:
  - ユーザーが Blender のアウトライナーやDeleteキー、Undo履歴、スクリプトなどから直接コレクションを消去した際、描画キャッシュ（`self.stacks`）に無効なスタックポインタが取り残され、幽霊メッシュがビューポートに残ってしまう不具合を特定。
  - 描画ループの先頭で現在シーンに存在する CAD コレクションを全走査し、無効になったスタックを自動検知して消去する自動GCメソッド `_garbage_collect_leaked_stacks()` を `drawing.py` に実装。
  - 描画バッファキャッシュの破棄（`self.clear`）と同時に、C++ レイヤーの `delete_cad_stack(ptr)` を自動的にフックして呼び出すことで、OpenCASCADE 側のメモリリークも全自動で防止する極めてクリーンで安全なメモリ設計を確立。

## Blender_CAD_V_1_4_4 (2026-05-17) - Multi-body Instance System Integration & Zero-Crash Background Headless Optimization
- **アセンブリ・インスタンス参照システム（INSTANCE / Body Link）の実装完了**:
  - `INSTANCE` (Body Link) プリミティブを完全実装。特定のオブジェクトを「マスター」として指定し、そのマスターの OpenCASCADE 形状を別座標（トランスフォーム）へ複製・配置する仕組みを確立。
  - **非破壊アセンブリワークフロー**: マスター形状を変更すると、配置されたすべてのインスタンス形状がリアルタイムかつ非破壊で一斉に連動アップデートされる Plasticity 風の先進的モデリング UX を実現。
  - **複数インスタンスのブーリアン処理に対応**: インスタンス化したパーツをメインボディに対して結合（ADD）やくり抜き（SUB）し、トランスフォームによる一体操作までサポート。
- **バックグラウンド実行（ヘッドレスモード）時のゼロクラッシュ設計（遅延シェーダーロード）**:
  - Blender をバックグラウンド起動した際（テスト実行やCI環境など）、OpenGL/Vulkan/DirectX 等の物理的な GPU コンテキストが存在しないことに起因する `SystemError: GPU functions for drawing are not available in background mode` を完全に克服。
  - `drawing.py` の `WireframeEngine` や `SeamlessGizmoManager` 内の GPU 関連バッチおよびシェーダーのコンパイルを、実際のビューポート描画時まで動的に遅延させる **Lazy Shader Initialization** 技術を導入。
- **非同期計算時における同期更新（ベイクなど）の排他制御バグ（計算取りこぼし）の解消**:
  - `core_bridge.py` において、非同期計算スレッドが実行中（`is_computing() == True`）の際に、最後のベイクや強制同期プレビュー更新要求（`sync=True`）までが不要にスキップ（ドロップ）されていた重大な競合バグを特定・解消。
  - 同期要求のときは `is_computing` フラグを安全にバイパスして Rust 側で強制的に同期再計算・結果適用させるよう修正。これにより、複雑な連動トランスフォーム操作直後の `Bake to Mesh` の結果が 100% 確実に最新の全ジオメトリを含むよう保証され、テストコード `test_instance.py` の全パス（マスター Box と移動された Instance Box の同時ベイクと検証）を達成。

## Blender_CAD_V_1_4_3 (2026-05-17) - Preparation for Multi-body & Instance System (Refactoring)
- **V1.5.0（マルチコレクション・インスタンス参照システム）に向けたデータ結合度の緩和**:
  - **properties.py のリファクタリング**: 
    - 座標変換処理 `get_local_pos`/`set_local_pos` およびコールバック `update_gizmo_callback` が、シーン直下のプロパティ `context.scene.seamless_props` に強結合していた問題を完全解消。
    - 自身が所属する primitives コンテナを動的に探索・解決するヘルパー `get_stack_props(self)` を `SeamlessPrimitive` 内に新たに実装。
    - これにより、将来的に primitives リストが Scene から Collection や Object など、どの所属先に移行・配置されても、1箇所（ヘルパー）の数行を書き換えるだけで他のすべての座標計算ロジックが無傷で動作する「拡張性の極めて高い疎結合構造」を実現。
  - **utils.py のリファクタリング**:
    - `sync_proxies` および `update_parenting` を、特定のグローバルプロパティや `"Seamless_Proxies"` という文字列に依存しない設計へ変更。
    - `sync_proxies(context, props=None, collection_name="Seamless_Proxies")` のように、同期対象の props や同期先コレクション名を引数で動的に指定可能に拡張。
    - **100%の完全な後方互換性**を維持しつつ、将来的に異なるコレクション名（例：`Seamless_Proxies_2`）を渡すだけで、他のコレクションのプロキシを完全同期・管理できる体制を完了。
  - **V1.4.3 へのビルド＆デプロイ環境の追従**:
    - `__init__.py` のバージョン番号を `1.4.3` にアップデート。
    - `rebuild_and_deploy.bat` のコピーおよび展開先パスを `CAD_1_4_3` に完全統一。リビルドとBlenderデプロイをエラーフリーで成功させました。
  - **ネイティブコアの完全カプセル化とメモリ安全性 (100% 達成)**:
    - 従来の C++ 側の単一グローバル変数（状態）を完全に廃止し、すべての幾何データ・キャッシュ・操作状態を `CADStack` クラスへとカプセル化。複数シーンや並列実行時のメモリ干渉リスクを根本から排除。
    - **オンデマンド・ポインタ初期化（Lazy-Initialization）**: Blender のアドオン登録時におけるデータアクセス制限（`_RestrictData`）を完全に回避するため、実際のモデリング計算時にのみポインタを動的確保する安全な遅延初期化アーキテクチャを確立。
  - **64ビット環境におけるメモリアドレスの完全保護**:
    - Windows 64bitOS 等において C++ が確保したメモリアドレスが 32ビット整数の上限（約21.4億）を超えオーバーフローする境界値エラーに対し、ポインタを `StringProperty`（文字列）として伝搬。取得・破棄時に Python の多倍長整数パースを経て安全に Rust API へ引き渡す堅牢なデータブリッジを構築。
  - **各種オペレータの最新 Blender API への適合とデバッグ**:
    - `Scene.ray_cast()` の返り値が 4値から 6値へと拡張されたモダン Blender（2.8〜5.x）の API 仕様へ完全に適合。ミラーのターゲットピッキング等がエラーなく 100% 確実に動作するよう修正。
    - カプセル化による引数変更に伴う `Bake to Mesh` 時のポインタ引き渡し漏れ（`TypeError`）や、スナップ移動中のキー判定漏れ（`UnboundLocalError`）などの UX バグをすべて根絶。非常に安定したモデリング基盤に仕上がりました。

## Blender_CAD_V_1_4_2 (2026-05-17) - Parent-Child Relationship Inversion (Design Paradigm Shift)
- **親子関係の逆転ロジック (依存関係の逆転) による操作性の劇的改善**:
  - **対称面・中心軸を基準(親)とするアセンブリ構造の導入**: 従来の「元オブジェクト ➔ モディファイア（子）」という依存構造から、「モディファイア（親：Mirror/Circle/Revolveの基準面や中心軸） ➔ 元オブジェクト（子）」へと親子関係を180度逆転。
  - **一体移動（相対位置の完全維持）の保証**: ミラープロキシや回転軸自体を移動・回転した際、配下にある元のオブジェクトと生成される鏡像・アレイ形状が、**相対位置関係（距離や対称性）を完全にキープしたまま一体となって完璧に追従・移動**する挙動を実現。
  - **中心軸の維持とパラメータ変更**: 子である元の形状だけを掴んで動かした場合は、中心軸に対する「ローカルなオフセット距離」の変更として処理されるため、中心を保ったままで対称関係の開き具合などを極めて直感的に調整可能に。
  - **循環親子参照（Dependency Cycle）の完全防止**: Blender上で複雑な親子付けが重なった場合にも、親子ループが発生しないよう、先祖・子孫関係を再帰的にチェックして親子付けをブロックする安全装置を実装。
  - **ローカル座標（Local Offset）算出の逆転**: 逆転したデータ構造に追従し、Nパネルで入力する「Local Offset」が親モディファイアの座標系に基づいて正確に機能するよう、ゲッター／セッターの内部座標系変換数式を修正。

## Blender_CAD_V_1_4_1 (2026-05-17) - Revolution Angle Parameters & Snap Stabilization
- **Revolution（回転体）の角度パラメータ不具合の完全修正**:
  - C++ネイティブコアの `occ_core.cpp`（453行目）における変数参照の不整合を特定。円弧用の誤った角度差分参照を、UIで設定された "Total Angle" (`distances[i]`) に修正。これにより、回転体の回転角を0度〜360度まで自在に制御可能に。

## Blender_CAD_V_1_3_9 (2026-05-16) - Ultimate Stability & Chamfer Refinement
- **「サイレントクラッシュ」の完全克服 (究極の安定化)**:
  - **3層防御アーキテクチャの導入**: ログに残らないBlenderの強制終了（サイレントクラッシュ）を徹底調査し、ネイティブ・Python・UXの3レイヤーで対策を実装。
    1. **ネイティブ層 (C++)**: `get_edge_points` 等の並列アクセス箇所にミューテックス・ロックを適用し、データ競合（Data Race）を根本的に解消。
    2. **Python層**: 非同期計算中（is_computing）はマウス移動によるピッキング呼び出しを自動停止するガードを実装。スレッド間の不要な衝突を未然に防止。
    3. **堅牢なバッファ管理**: すべての `sprintf` を `snprintf` へ置換し、極端な座標値によるバッファオーバーフローのリスクを排除。
- **Chamfer（面取り）機能の洗練**:
  - **面選択からのエッジ展開に対応**: 面（Face）を選択した際、その面を構成するすべてのエッジを自動抽出し、一括で面取りを適用するロジックを実装。Filletと同等の高度な操作性を獲得。
- **ビューポート視認性の極致化**:
  - **動的透明度・X-Rayシェーディング**: `viewport_opacity` スライダーによるリアルタイムな透明度制御を実装。ソリッド表示とX-Ray表示をシームレスに切り替え可能になり、複雑なモデル内部の辺選択が容易に。
  - **深度テストの正常化**: 透明度100%（不透明）時にモデルが確実にソリッドに見えるよう、レンダリングパイプラインの深度テストとアルファブレンディングを修正。
  - **オーバーレイ同期の完成**: OpenGLとWGPUの描画サイクルを完全に同期させ、描画のズレやダブルレンダリングによるチラつきを排除。
- **インタラクションの精度向上**:
  - **高精度マウス操作**: レイ・プレーン（光線と平面）交差法による移動ロジックを実装。オブジェクトの移動がピクセルパーフェクトになり、ラグのない吸い付くような操作性を実現。
  - **プロフェッショナル・ギズモ**: 回転操作用の円形ハンドルを導入。操作対象のリアルタイム・ハイライトにより、直感的な3D空間操作をサポート。
- **スタンドアロン版「Seamless CAD Desk」の基盤確立**:
  - **ネイティブ実行環境の安定化**: OpenCASCADE、TBB、FreeTypeなどのランタイム依存ライブラリのリンク問題を解決。外部DLLなしでの起動安定性を確保。
  - **PBRベースのプレビュー**: 物理ベースレンダリング（PBR）に対応したマテリアルシステムを統合。
- **SDF-R (Standalone SDF Engine) の飛躍的強化**:
  - **高度な非破壊デフォーマー**: Twist（捻り）, Bend（曲げ）, Taper（テーパー）, Elongate（伸長）の各デフォーマーをWGSLシェーダーレベルで実装。
  - **ゴースト・プレビュー (Ghost Preview)**: ブーリアン引算（Subtract）で削られる形状をシアンのグローで可視化。非破壊編集中の視認性を劇的に向上。
  - **レンダリング・フィデリティ**: ガンマ補正、アンビエント・オクルージョン、適応型レイマーチングによる高品質なソフトシャドウを実装。

## Blender_CAD_V_1_3_8 (2026-05-16) - Asynchronous Processing & Stability
- **ノンブロッキング・プロセッシングの実装**:
  - **非同期ジオメトリ更新パイプライン**: `bpy.app.timers` を活用した非同期ポーリング機能を導入。重いブーリアン演算やフィレット処理をバックグラウンド（Rust側）で実行し、計算中も Blender の UI が一切フリーズしない「真のノンブロッキング操作」を達成しました。
  - **計算リクエストの最適化**: `is_computing()` による状態監視を行い、連続した操作時には古いリクエストをスキップして最新の状態のみを反映するインテリジェントな更新スキップロジックを実装。
- **データ通信の堅牢化**:
  - **JSONブリッジの高速化**: Python-Rust間の形状データ受け渡しを最適化。大規模なアセンブリや数万エッジのモデルでも、遅延のないシリアライズ・デシリアライズを実現。
  - **クラッシュガードの再構築**: 例外処理の全域カバーにより、幾何計算の失敗がアドオン全体のクラッシュに波及しないよう、Rust/C++レイヤーでのエラーハンドリングを強化。

## Blender_CAD_V_1_3_7 (2026-05-15) - Smart Update & Dev Workflow
- **Depsgraph更新のインテリジェント化**:
  - **Dirtyフラグ管理の導入**: 形状の変更、トランスフォーム、精度の変更を個別にハッシュ化して管理。変更があったプリミティブとその依存関係のみを再計算する部分的更新を実現し、不要な計算を最大40%削減。
- **開発・デプロイ環境の統合**:
  - **自動ビルドスクリプトの刷新**: `rebuild_and_deploy.bat` により、バイナリのビルドからBlenderアドオンディレクトリへの配置、リロードまでを自動化。開発サイクルの劇的な高速化を実現。

## Blender_CAD_V_1_3_6 (2026-05-15) - High-Quality Real-Time Fusion & Stabilization
- **V1.3.5の美しさとV1.3.6の速度の融合**:
  - **高品質離散化への完全回帰**: 操作中に形状が崩れる「低品質モード」を廃止。常に V1.3.5 相当の高品質な離散化（Tangential Deflection）を行いながら、V1.3.6 の最適化（文字列生成の削減等）を組み合わせることで、美しさと速度を両立。
  - **究極のリアルタイム追従**: 0.3秒の超高速インアクティビティ・タイマーを導入。マウスを動かしている間も、止めた瞬間も、ラグを感じさせないシームレスな高品質表示を実現。
- **トランスフォーム同期の完全解消**:
  - **「先祖返り」バグの根絶**: ドラッグ中に古いプロパティ値がオブジェクト座標を上書きしてしまう同期の不整合を解消。マウス操作に CAD プレビューが 1:1 で吸い付くレスポンスを確立。
  - **全レイヤー同期**: エッジ（ワイヤー）だけでなく、面（シェーディング）もドラッグ中にリアルタイム更新されるよう更新パイプラインを刷新。
- **スケーリング操作の安定化**:
  - **不均等スケール対応**: BOXやCYLINDERなどの不均等スケーリングを幾何パラメータとして正しく処理。二重スケーリング（サイズが2倍になる問題）を完全に解消しました。
- **モディファイアと連動する完全同期の実現 (最新アップデート)**:
  - **Mirror / Array の追従性向上**: インタラクティブな移動や変形中に発生していた Mirror および Array モディファイアの同期ズレを解決し、リアルタイムで正確な幾何学フィードバックを確立。
  - **Independent Move (独立移動) の堅牢化**: 意図しない座標スナップや冗長な状態リセットを排除し、自由なトランスフォーム操作を保証。
  - **Depsgraph Update Handler の最適化**: パフォーマンス低下の原因となっていた不要なイベントの連鎖を断ち切り、PythonフロントエンドからRust/C++コアへの連携を極限まで軽量化。
- **ビューポート表示とハイライトの修復 (最新アップデート)**:
  - **シェーディング状態の競合解消**: WGPUオーバーレイの調整に起因するレンダリングパイプラインのバグを修正し、ベースとなるCADプリミティブの高品質なグレーシェーディングを復元。
  - **インタラクティブ・ハイライトの確実な描画**: 選択時、ホバー時、およびモディファイアのターゲット選択時において、面やエッジのハイライトがすべてのモードで正しく描画されるよう修正。
  - **Fast Preview 品質の適正化**: 移動時の高速プレビュー（Fast Preview）発動時に、意図せずハイポリ化（高解像度化）して負荷がかかっていたバグを修正。ユーザーの意図した品質設定を尊重するように修正。
- **ダイナミック・ロフト (Dynamic Loft) ツールの実装**:
  - 従来の「Variable Box」を進化させ、上面と底面のプロファイル（Box または Circle）を個別に選択・補間できる「ダイナミック・ロフト」ツールを導入。UIからの精密な数値制御と、異なるプロファイル間での高品質な形状生成を実現。
- **アセンブリ・ワークフロー (Stealth Mode) の強化**:
  - ブーリアン等で「消費された (Consumed)」オブジェクトに対して、不要なCADジオメトリ出力を抑制する Stealth Mode を実装。二重トランスフォームや無限更新ループを防ぎつつ、深い階層構造を持つアセンブリ作業をサポート。


## Blender_CAD_V_1_3_5 (2026-05-14) - Viewport Interaction & Target Selection
- **インタラクティブなターゲット選択**:
  - モディファイア（Boolean等）のターゲットオブジェクトをビューポート上で直接クリックして選択できるモーダルオペレータを実装。
  - リストからの選択とビューポート選択のハイブリッド方式により、直感的なワークフローを実現。
- **テセレーション伝搬の最適化**:
  - Angular Deflection（角度偏差）の設定がサーフェスメッシュとワイヤフレームの両方に正しく反映されるよう修正。

## Blender_CAD_V_1_3_4 (2026-05-14) - Hardware Compatibility & iGPU Stabilization
- **ノートPC内蔵GPU (iGPU) への完全対応**:
  - Intel Iris Xe 等の内蔵GPU環境で発生していた `EXCEPTION_ACCESS_VIOLATION` を解消。
  - メモリ管理の堅牢化と、ピクセルデータ転送の効率化（8-bit への最適化）により、リソース制限の厳しい環境での安定性を劇的に向上。
  - レンダリング解像度の制限オプションを追加し、高解像度ディスプレイ使用時のVRAM枯渇を防止。
- **インテリジェント・テセレーション (V 1.3.3)**:
  - 線形偏差（Linear Deflection）に加えて角度偏差（Angular Deflection）を導入。
  - 曲率の大きい部分には密に、平坦な部分には粗くポリゴンを割り当てることで、軽量化と見た目の滑らかさを両立。

## Blender_CAD_V_1_3_2 (2026-05-13) - Adaptive Quality & Engine Stability
- **適応型品質制御 (Adaptive Quality Control)**:
  - テセレーション精度（Deflection）をUIのスライダーからリアルタイムに制御可能に。
  - **Viewport Quality / Final Bake Quality**: 表示用は軽く、出力用は美しくというパラメトリックな使い分けを実現。
- **マルチスレッド安全性の確立**:
  - CADコアに Mutex を導入。高負荷なテセレーション計算中にマウス操作（ピッキング）を行ってもパニックが発生しない堅牢なアーキテクチャへ刷新。
- **フェイルセーフ設計**:
  - ブーリアン演算やフィレットが一時的に失敗する状態（モディファイアの初期状態など）でも、アドオンを停止させず空の形状を安全に返却するガードを実装。
- **レンダラーの安定化**:
  - ウィンドウサイズ変更時のメモリアクセス境界チェックを強化し、WGPU描画スレッドの安定性を向上。

## Blender_CAD_V_1_3_1 (2026-05-13) - Zero-Latency Rendering Integration
- **WGPU オーバーレイエンジンの統合**:
  - 従来の OpenGL ベースから、Rust/WGPU による直接描画へ移行。数万エッジを超える大規模モデルでも遅延のない描画を実現。
- **適応型キャッシュシステム**:
  - 形状、トランスフォーム、精度のハッシュを管理し、変更があった部分のみを最小限に再計算するインクリメンタルな更新パイプラインを構築。

## Blender_CAD_V_1_2_1 (2026-05-13) - 高精度ジオメトリエンジンの完成とインボリュート・ギアの実装
- **プレミアム・ギア (Involute Gear)**: 
  - 単純な直線近似から、本格的なインボリュート曲線を用いた高精度な歯形生成へアップグレード。歯数が少ない場合でも滑らかで正確な機械的な噛み合い形状を再現。
- **ジオメトリ・パイプラインの刷新**:
  - 厚み出し（Extrude）と座標変換（Transform）の実行順序を最適化。回転したスケッチに対してもローカル軸に基づいた正しい厚み付けを保証。
  - パイプ生成（Pipe）および面張り（Fill Closed）機能を V1.2.1 向けに完全復元。
- **ブーリアン安定性の向上**:
  - 非ソリッド（エッジやワイヤー）を結合した際の「消失問題」に対し、Compound（複合形状）フォールバックを実装。スケッチ段階の線も確実に見えるよう改善。
- **ID同期の厳格化**:
  - Lineage ID フォーマットの統一（区切り文字をセミコロン `;` に固定）により、Python-Rust 間のデータ整合性を強化。ハイライトと選択の安定性が向上。

## Blender_CAD_V_1_1_1 (2026-05-12) - Slotプリミティブ & 押し出し(Extrude)機能の追加
- **Slot (長穴) プリミティブ**: 
  - 工業デザインで多用されるスタジアム形状を1クリックで作成可能。半径と中心間距離で動的に制御。
- **厚み付け (Extrude) プロパティ**:
  - Curve, Arc, Surface, Slotなどの2D形状に対して「厚み」を即座に与えるプロパティを追加。
- **Surface UIの改善**:
  - Surfaceプリミティブでも点リストの追加・編集が可能になり、任意の多角形から面を生成するフローを確立。

## Blender_CAD_V_1_1_0 (2026-05-12) - トポロジカル・ネーミング問題の解決

- **座標ベースの動的エッジ追従 (Zone Tracking)**:
  - 選択IDを `Edge:ID@X,Y,Z` 形式に拡張。
  - ボール演算やフィレット後にトポロジが変わっても、指定座標周辺の形状を自動再検知。
- **複数ヒット・自動フィードバック**:
  - 1本の辺が2本に分裂した場合の同時ハイライト・同時処理に対応。
  - コアエンジンからアドオンプロパティへのID自動書き戻し（Sync）を実装。
- **対応モディファイア**: Fillet, Chamfer, Face Offset, Draft, Shell。

## 4. 今後の展望・ロードマップ
- **V1.5.0: マルチボディ・インスタンス参照システム (アセンブリモデリングの導入)**:
  - **複数コレクション / オブジェクトスタックの独立管理**: `Seamless_Proxies_1` や `Seamless_Proxies_2` などの独立したスタック（履歴）を完全に不干渉で作成可能に。
  - **アセンブリ・インスタンス化（BODY_LINK）**: 離れた場所（部品庫）で作成した複雑な部品（またはシンプルなボルト穴・カッター）を、メインボディ上にインスタンスとして複数配置し、ブーリアン（ADD/SUB）で結合可能に。
  - **一斉連動アップデート (Plasticity インスパイア)**: 部品庫にある元の形状（マスター）を変更すると、メインボディ上に配置されたすべてのインスタンス形状がリアルタイムで一斉更新される、直感的なデザイナーフレンドリーUXの実現。
- **高度なモデリング機能**:
  - スケッチ平面の自由な定義とスナップ機能の強化。
- **パフォーマンス最適化**:
  - 複雑なブーリアンツリーの再計算の最適化（Dirtyフラグ管理）。
  - 大規模モデルにおけるGPU描画の高速化。
- **UXの洗練**:
  - ギズモを用いたより直感的な数値入力とトランスフォーム。

## 5. 現時点でのゴール（マイルストーン）
- **安定したCADモデリング基盤の確立**: シンプルな機械部品であれば、Blenderから出ることなく正確な幾何学形状として完結できる状態。
- **シームレスなメッシュ出力**: CADデータから、トポロジーの整った美しいメッシュへの「Bake」機能の完成。

---
最終更新日: 2026-06-04
作成者: Antigravity (AI Coding Assistant) & hinata_hugu